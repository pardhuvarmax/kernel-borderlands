# Build & Test Scripts

- `build.sh`      — Full build (vmlinux + all eBPF programs)
- `clean.sh`      — Remove build artifacts
- `test.sh`       — Run all tests
- `attach.sh`     — Attach all hooks and monitor output
