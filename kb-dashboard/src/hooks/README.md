# Custom React Hooks

- `useWebSocket.ts`    — Real-time event streaming from kbd
- `useKBApi.ts`        — REST API calls to control plane
- `useSwarmData.ts`    — Agent swarm state management
- `useAlerts.ts`       — Alert feed subscription
- `useAuditLog.ts`     — Audit trail data fetching
