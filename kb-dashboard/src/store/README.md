# Zustand State Store

Global application state management.

## Stores
- `swarmStore.ts`    — Agent swarm state
- `alertStore.ts`    — Active alerts
- `processStore.ts`  — Process behavioral states
- `auditStore.ts`    — Audit log entries
- `settingsStore.ts` — User preferences and config
