# Reusable Components

Shared UI components used across multiple pages.

## Planned Components
- `Sidebar.tsx`         — Navigation sidebar
- `AlertCard.tsx`       — Single alert display
- `AgentBadge.tsx`      — Agent role badge
- `ZoneBadge.tsx`       — Safe/Suspicious/Borderlands badge
- `ScoreBar.tsx`        — Risk score progress bar
- `ConsensusCard.tsx`   — Voting progress display
- `AuditEntry.tsx`      — Single audit log entry
- `MetricCard.tsx`      — KPI metric display card
- `SwarmGraph.tsx`      — D3.js force graph (topology)
