# TUI Entry Point

Main entry point for the KB TUI.
Starts either SSH server (production) or direct terminal (dev).

## Modes
- `--ssh`    — Serve TUI over SSH (wish, port 2222)
- `--local`  — Run directly in current terminal
