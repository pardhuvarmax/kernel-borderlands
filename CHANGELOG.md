# Changelog

All notable changes to the **Kernel Borderlands** project are documented below.

---

## [Unreleased] - 2026-09-14

### Fixed
- **`docs/architecture/boot_sequence_spec.md` still described `kbd` as the thing that loads `kbd_sensor`'s eBPF hooks, in three separate places, with the ordering backwards** (the mermaid sequence diagram, Phase 1's numbered list, the `T+5.00s`/`T+5.50s` chronological timeline, and the "eBPF Hook Loading Failure" fail-safe scenario in §4): an earlier pass this session added `kb-sensor.service` and a pointer note ahead of §3's unit-file listing, but never went back to correct the doc's own narrative text, which still said `kbd` itself compiles/loads/attaches the sensor's hooks — and worse, placed that step *before* `kbd`'s readiness notification, which is backwards from the real systemd chain (`kb-sensor.service`'s `Requires=kbd.service` means it cannot even attempt to start until after `kbd` reports ready). Corrected all four spots: added a `Sensor` participant to the mermaid diagram and moved its lane after `kbd`'s readiness notification; swapped Phase 1's step 3/4 order and reassigned eBPF loading to `kbd_sensor`; swapped the `T+5.00s`/`T+5.50s` timeline entries the same way; rewrote the eBPF-failure scenario in §4 to describe the real mechanism (a `kb-sensor.service` crash-loop blocks `kb-checker.service` via its `Requires=`, not a `kbd.service` readiness failure — `kbd` itself is unaffected). Also updated §2 Phase 2 step 1 to mention `kb-checker.service`'s `Requires=kb-sensor.service` alongside `kbd.service`, which the same earlier pass added to the unit file but never reflected in the narrative. [Claude Code]
- **kbd never sent `sd_notify(READY=1)` despite `kbd.service` declaring `Type=notify` — the entire boot dependency chain silently never proceeded** (`kb-control-plane/cmd/kbd/main.go`, `go.mod`): found by actually booting `packaging/systemd/*.service` in a privileged systemd-enabled Docker container (`docker run --privileged --cgroupns=host -v /sys/fs/cgroup:/sys/fs/cgroup:rw ...`) instead of only running `systemd-analyze verify` — that tool only checks unit-file syntax, it never runs `ExecStart`, so it couldn't have caught this. Confirmed via `journalctl -b`: `kbd.service` sat forever in `activating (start)`, and `kb-checker.service`/`kb-sensor.service` both correctly refused to start with "Dependency failed" (their `Requires=kbd.service` was working exactly as designed — it just never resolved because `kbd` never told systemd it was ready). Root cause: zero `sd_notify` calls anywhere in `kb-control-plane` (confirmed via grep before writing any fix). Added `github.com/coreos/go-systemd/v22`'s `daemon.SdNotify`, called from a new `notifySystemdReady()` in `main.go` after `cp.Start()` returns — implements exactly what `docs/architecture/boot_sequence_spec.md` §2 Phase 1 step 4 already claimed happened ("kbd verifies its local sockets are listening and sends a ready signal back to systemd"), which was previously just narrative, not code. Polls for `kbd.sock`/`kbct.sock`'s existence (bounded, 3s) before notifying, since `internal/ipc`'s two listeners bind inside their own goroutines and aren't synchronously confirmed by the time `cp.Start()` returns — a real, still-open minor race, left as-is rather than refactoring `Listener`'s API under this change; `SdNotify` is a no-op when `NOTIFY_SOCKET` isn't set, so this is safe for every non-systemd dev/manual run too. Reverified in the same container after the fix: `kbd.service` now reaches genuine `active (running)`.
- **`kbd.service`'s real `--db /var/lib/kbd/state.db` path crash-looped forever — the directory never gets created by anything** (`packaging/systemd/kbd.service`): also found via the same real boot test, before the `sd_notify` fix was even in place — `journalctl` showed `unable to open database file: no such file or directory`, repeating every `RestartSec=3`. Fixed with `StateDirectory=kbd`, the systemd-native equivalent of the same unit's existing `RuntimeDirectory=kb` for `/run/kb`, rather than adding a manual `mkdir` to `scripts/setup/provision.sh` (ties the directory's lifecycle to the unit itself, the idiomatic systemd answer to exactly this).
- Real safety behavior, not a bug — noted for anyone re-running this test: with both fixes in place, `kb-checker.service`'s actual Tampering Containment Protocol (Scenario A, boot_sequence_spec.md §5) then fired for real inside the bare container — it found `contained_pids_map not found in kernel` and the AADS/Ray swarm unreachable (neither is running in a bare packaging-only test) and correctly ran its full documented fail-safe: killed `kbd_sensor`, detached the eBPF links, and quarantined the container's network with real `iptables` rules. A genuinely "all green" boot test needs the full stack (including a real AADS swarm) running together, not just `kbd`/`kb-sensor`/`kb-checker` in isolation — this is the intended conservative fail-closed behavior working correctly, not a packaging defect to fix.
- Independently confirmed real (not stubbed): `kbd_sensor` run directly inside the same privileged container loaded all 6 real eBPF hooks, attached real uprobes into the container's `libssl.so`/`libgnutls.so`, and streamed genuine host-kernel telemetry (containers share the host kernel) — eBPF loading itself works under `--privileged` Docker on this host's 6.8 kernel, not just in theory.

### Added
- **kb-dashboard's two placeholder pages (Rogue Management, Settings) wired to real backend data — new kb-aads status server, new kbd endpoints** (`kb-aads/api/status_server.py`, `kb-aads/main.py`, `kb-control-plane/internal/policy/policy.go`, `kb-control-plane/internal/controlplane/http.go` + `http_test.go`, `kb-op/kb-dashboard/src/App.tsx`): a survey of the existing HTTP surface confirmed all 5 already-wired dashboard pages' 7 API routes (`/api/processes`, `/api/alerts`, `/api/logs`, `/api/services`, `/api/metrics`, `/api/events` SSE, `/api/isolate`/`/api/restore`) already exist in `kbd` and match the dashboard's expectations exactly — nothing to fix there. The two placeholder pages, however, had no backend at all:
  - **Rogue Management**: no mechanism previously let any HTTP client see Ray-side agent state — `kb-aads` only ever dials *out* to `kbd` over gRPC (`comms/grpc_client.py`), never the reverse. Added `kb-aads/api/status_server.py`, a minimal stdlib `http.server` started on a background thread from `main.py` (default `127.0.0.1:8601`, overridable via `KB_AADS_STATUS_HOST`/`KB_AADS_STATUS_PORT`) exposing `GET /agents` — `SwarmRegistry.list_all()` plus each handle's `get_status()` (role/registry status/uptime/error_count). Deliberately does **not** call `JudgeAgent.assess_severity` per agent on every request — its liveness check does `asyncio.sleep()` per agent, which would make every dashboard page load block for `N × liveness_check_seconds`; the page's "Health Ladder" tier legend is now explicitly labeled reference-only rather than presenting fake computed tiers as live. Added `kbd`'s `GET /api/agents` (`handleAgents`, http.go) as a thin proxy (`KB_AADS_API_ADDR`, default `http://127.0.0.1:8601`) with a 2s timeout, so "swarm not running" (routine in dev) surfaces as a clean 503, not a hang.
  - **Settings**: revived two previously write-only `policy.Engine` fields (`defSus`/`defBor` — the type's own comment noted they're parsed but not consulted by Go's zone-classification logic, which moved to `kb_scoring.c`) as new read-only accessors (`DefaultSuspiciousThreshold`/`DefaultBorderlandsThreshold`), explicitly commented as display-only so nobody later assumes setting them changes enforcement behavior. Added `kbd`'s `GET /api/policy` (parsed defaults + sensitive-path count) and `POST /api/policy/reload` (gated behind the same bearer-token pattern as isolate/restore) — the latter is the HTTP-facing equivalent of the existing `ReloadPolicy` gRPC RPC, calling the same already-tested `reloadPolicyFromDisk()` rather than duplicating its logic. Settings page now shows live values and a working "Reload from disk" button.
  - New Go tests (`http_test.go`): `TestHandlePolicy_ReturnsParsedDefaults`, `TestHandlePolicy_RejectsNonGet`, `TestHandlePolicyReload_GatedBehindAPIToken`, `TestHandlePolicyReload_SucceedsWithValidToken`, `TestHandleAgents_ReturnsServiceUnavailableWhenAADSUnreachable`. Full `kb-control-plane` suite passes. Verified live (not just unit tests): built and ran a real `kbd` binary against a real `policy.yaml`, confirmed `/api/policy` returns real parsed values and `/api/agents` returns a clean 503 with kb-aads not running — then confirmed via `go test` since the sandbox has no root to bind `/run/kb`'s required UDS sockets for a longer-lived run. `npm run build` for the dashboard and `python3 -m py_compile` for the new Python file both clean. [Claude Code]
- **kb-dashboard: sidebar expanded into a 4-group, 12-page control panel; fixed a squeezed/invisible Threat Feed panel** (`kb-op/kb-dashboard/src/App.tsx`): the user's reference screenshot's real ask turned out to be sidebar breadth, not just card styling — its 4 labeled groups (Core Monitoring / Swarm Management / Intelligence & Communication / System & Governance) each hold several pages, where this dashboard had 5 flat items. Restructured `NAV_GROUPS` into **Core Monitoring** (Processes, new **Zone Topology**, Alerts, Telemetry), **Platform Management** (Services, new **Containment**), **Intelligence & Consensus** (new **Consensus Log**, new **Rogue Management**), **System & Governance** (Console, new **Settings**) — deliberately not a 1:1 port of the AADS-only reference IA, since the whole point of the earlier grouping work was keeping the swarm as one subsystem among several. Every new page is built from state already in the app, no fabricated data: **Zone Topology** buckets `processes` into 4 per-zone panels; **Containment** is `processes` filtered to BORDERLANDS/QUARANTINED with the existing isolate/restore actions, plus a containment-history log filter; **Consensus Log** filters the existing `log` array for `[AADS]`-tagged lines with a verdict-count summary. **Rogue Management** and **Settings** are the two pages with no live backend today — rather than fake data, both explicitly say so in-page (Rogue Management names the real `kb-aads` JJE courthouse methods this would eventually call — `JudgeAgent.assess_severity`/`enforce_verdict`, added in an earlier session — and states plainly that no `/api/agents`-style endpoint exists yet to back it). Also fixed a real layout bug found via user report: the Processes view's Threat Feed panel sat in the right column sandwiched between Service Health and the Audit Console as a `flex:1` in a column that had no room left, rendering at ~0px height — moved it to the left column below Tracked Processes with a fixed `maxHeight`, where it's actually visible. Verified: `npm run build` compiles clean. [Claude Code]
- **kb-dashboard: fleshed out Alerts/Services/Telemetry/Console views to match Processes' density ("more full control panel")** (`kb-op/kb-dashboard/src/App.tsx`): the clean-console redesign above only made the Processes view feel like a full panel — the other four still read thin next to it. Added one summary panel per view, all built from data already flowing through the app (no fabricated metrics): **Alerts** gained a Severity Breakdown panel (critical/warning/info counts) above its table; **Services** gained a Platform Composition panel (per-service language tags — Go/Rust/C-eBPF/Python — plus an active/down count summary) beside its Audit Console; **Telemetry** gained a Zone Summary panel next to the trend chart, and a 4th metric tile (**Event Rate**) restoring a stat that had been on the topbar before the earlier redesign trimmed it and had nowhere else to live; **Console** gained a 4-tile stat strip (total/error/warning/confirmed-OK line counts, derived from the existing log array) above the terminal. Verified: `npm run build` (`tsc -b && vite build`) compiles clean. [Claude Code]
- **kb-dashboard: redesign changed direction from black-neon to a clean flat console (per direct user reference screenshot), grouped sidebar nav added** (`kb-op/kb-dashboard/src/{index.css,App.tsx}`): the black-neon pass above was superseded once the user shared an actual screenshot of the target reference (a flat, near-black, minimal SOC console with grouped sidebar sections, pill-style active nav, monochrome stat cards, and colored status dots used sparingly rather than glow everywhere) — no neon glow, no grid/scanline texture, `Inter` instead of `Space Grotesk`, larger border-radius (16px cards), nav active state is now a solid light pill (not a left-accent bar), sidebar nav restructured into three labeled groups (**Core Monitoring**: Processes/Telemetry, **Threat Response**: Alerts, **Platform**: Services/Console) — grouping chosen specifically so the AADS/swarm subsystem reads as one part of the platform, not the whole product (the user's explicit "not just AI" note), matching the reference's grouped-sidebar pattern without inventing nav items that don't correspond to real views. Added a per-view `<h1>` section heading (e.g. "System Overview", "Threat & Alert Center") mirroring the reference's plain heading above its stat cards; stat cards restructured to a label+icon top row over a large value (icon no longer sits in a colored chip — muted/neutral, matching the reference's monochrome card style), with a light progress bar added to the Safe Zone card (percentage of tracked processes) as the one card that has a natural percentage, mirroring the reference's "Health Index" bar. Verified: `npm run build` compiles clean. [Claude Code]
- **kb-dashboard: black-neon visual redesign, CSS-only (superseded above)** (`kb-op/kb-dashboard/src/index.css`, one one-line JSX fix in `src/App.tsx`, new `kb-op/kb-dashboard/design/dashboard-redesign-figma-prompt.md`): the existing dark-blue "admin panel" theme (flat `#0a0c10`/`#3b6fff`, Inter/JetBrains Mono) read as generic, not as a security operations console for the whole KB platform. Redesign is scoped to look only — no new views, nav items, or API calls; every class name already existed in `App.tsx` and drives its styling through CSS custom properties (including inline SVG props passed to `recharts`), so this was achievable as a token/component-style rewrite of `index.css` alone. New palette: true near-black background (`#050507`) with a faint fixed grid+vignette texture, cyan primary accent (control plane), violet reserved for AADS/swarm data (keeps "AADS is one subsystem, not the whole product" visually true), green/amber/red status colors, glow (`box-shadow`/`text-shadow`) applied only to live/active elements rather than uniformly. Typography swapped `Inter` → `Space Grotesk` for UI text, kept `JetBrains Mono` for data/logs. One functional gap found while wiring it up: `.score-bar-fill`'s inline style only set `background`, not `color`, so a `currentColor`-based glow had nothing to key off — fixed by also setting `color: fill` in `App.tsx`. Also produced a companion design-brief doc (`design/dashboard-redesign-figma-prompt.md`) mapping every current view/component for anyone taking this into Figma for further iteration. Verified: `npm run build` compiles clean (`tsc -b && vite build`), no new warnings; visual verification against the requested reference (`nimbus-bubble-19089060.figma.site`) could not be done directly — that URL is a Figma-canvas SPA that returns no fetchable content — so this is unverified against the reference pixel-for-pixel and was left running on a local dev server for manual comparison instead. [Claude Code]
- **Packaging foundation: real systemd units, `make install`, and host provisioning — materializes `docs/architecture/boot_sequence_spec.md`** (`packaging/systemd/*.service`, `packaging/ssh/`, `packaging/README.md`, top-level `Makefile`, `scripts/setup/provision.sh`): the boot sequence spec fully described the intended systemd dependency chain and `/etc/kb`/`/run/kb` layout, but none of it existed as real files — no `.service` file anywhere outside markdown code blocks, no `make install` target for any subsystem, no script creating the `kb` group/`operator` user/`/etc/kb`. Scoped to **foundation only** per explicit direction: materialize the unit files, add install targets for the subsystems with real binaries (`kb-core`, `kbd`, `kb-checker`, `kbctl`, `kb-tui`), and write host provisioning — `kbagents`/`kbopd` packaging (kb-aads/kb-dashboard have no compiled-binary story at all today) and giving `kbd` a real `--config` flag are deferred to a follow-up.
  - **Real gap found and fixed, not in the original scope ask**: the spec never defined a systemd unit for `kbd_sensor` (the C/eBPF binary), even though its own §5 Scenario A already referenced stopping "`kb-sensor.service`" without ever defining one. Confirmed via source (not just docs) that `kbd_sensor` (C) and `kbd` (Go) are two fully independent OS processes — `kbd` binds `kbd.sock` as the server (`kb-control-plane/internal/ipc/listener.go:134`), `kbd_sensor` connects as a client (`kb-core/userspace/bridge/kb_bridge.c:83`); neither spawns the other. Without a dedicated unit, the eBPF sensor would never actually start under systemd. Added `packaging/systemd/kb-sensor.service` (`After=kbd.service`, `Requires=kbd.service`) and updated `kb-checker.service`'s dependency to also require it.
  - `kbd.service`'s `ExecStart` corrected from the spec's `--config /etc/kb/config.yaml` (not a real flag — `kb-control-plane/cmd/kbd/main.go` has no `--config`) to its actual `--db`/`--policy`/`--rules`/`--workloads` flags, pointed at `/etc/kb/`.
  - `kbagents.service`/`kbopd.service` are included transcribed as-is from the spec, carrying forward its own honest "not yet functional, documents a target convention" framing.
  - `sshd@kb-operator.service`, `kb-operator.conf`, and `kb-tui-session-wrapper.sh` transcribed verbatim — these are already real per the spec's §2.11 STATUS: DONE migration.
  - `scripts/setup/provision.sh` (new, idempotent, requires root): creates the `kb` group, `operator` system user, `/etc/kb`, and the SSH host key — closes the gap `scripts/setup/install.sh` explicitly disclaims ("does NOT... create /run/kb, or touch /etc/kb"). Written and made executable but **not executed** against this dev machine — creating real system users/groups is an invasive, hard-to-reverse host action out of scope to do unprompted.
  - Removed the stray, tracked `kb-op/kbctl/kbctl` compiled binary (`git rm --cached`, added to `.gitignore`) — inconsistent with `kb-mcp`'s binary and `kb-control-plane/bin/kbd` (both already gitignored), and now redundant/build-masking now that `make install` provides a real way to produce it.
  - `docs/architecture/boot_sequence_spec.md` updated with a pointer to `packaging/systemd/` as the real source of truth and an explanation of the two corrections above, leaving the historical unit-file text below it unchanged per this repo's established correction-note convention.
  - **Unrelated pre-existing build failure fixed in passing, required to verify `make install`**: `kb-op/kb-tui/src/demo.rs`'s mock `Alert` construction was missing the `protected_workload`/`owner_team`/`justification`/`policy_id` fields added to the wire struct by earlier CWP work, so `kb-tui` failed to compile at all. Added the four fields with inert defaults (this is fake demo-mode data, not real telemetry).
  - Verified: `make install DESTDIR=/tmp/kb-install-test PREFIX=/usr` builds and installs all five real binaries cleanly into a scratch directory; `systemd-analyze verify` against every new unit file reports only "binary not found at /usr/local/bin" (expected — nothing installed system-wide yet), no syntax errors; `bash -n`/`sh -n` on `provision.sh` and the SSH wrapper script both clean. [Claude Code]

### Removed
- **NOMAD design-proposal docs deleted — NOMAD now has its own separate repository** (`docs/features/hardware-ext/` in full: `README.md`, `nomad-hardware-appliance.md`, `nomad-fms.md`, `nomad-cheap.md`, `nomad-expense.md`, `nomad-rootkit-resistance.md`; `latex/nomad/` in full: `main.tex`, `main.pdf`, `abstract.tex`, `abstract.pdf`): a stale-docs audit found these files still described NOMAD as "Proposed — not implemented, no code exists," contradicting the "actively developed in parallel, in its own repository (`github.com/pardhuvarmax/the-nomad`)" framing already established in `latex/paper/main.tex`'s NOMAD subsection. Rather than update eight files to a framing that says "this design lives elsewhere now," removed the design-proposal docs themselves from this repository — they belong in `the-nomad` going forward, not here. Updated the three docs that indexed/described them (`docs/README.md`, `docs/features/README.md`, `latex/README.md`) to point at the separate repository instead of the removed local files. `CHANGELOG.md`'s own historical entries for this work (the original NOMAD proposal, the OAN→NOMAD rename, the rootkit-resistance doc, etc.) are left untouched as historical record, per this repo's established convention — this is a removal of current-state docs, not a rewrite of history. `latex/paper/main.tex` needed no changes: it already only links to the external repository, never to the local files removed here. [Claude Code]

## [Unreleased] - 2026-09-07

### Documentation
- **Jury policy's reported held-out test accuracy (99.4%) traced to an unlogged earlier session run; re-run for independent verification, found run-dependent** (`kb-aads/marl/README.md`, `kb-aads/marl/checkpoints/jury_ppo`, `latex/paper/main.tex`): asked whether the paper's table values are real and accurate. Systematically re-checked every quantitative table against current source/live re-execution rather than assuming: socket table, ADFA-LD category mapping, and Go/Python test counts (128/68) all matched exactly. The Containment before/after per-level accuracy table (60.0%/70.4%/93.3%/etc.) traced exactly to a real, persisted `kb-aads/marl/session.log` — confirmed genuine, not fabricated. The Jury table's 99.8%/99.4% figures, however, had no persisted log backing them (only prose in `README.md`/`CHANGELOG.md` from an earlier session's live run) — re-ran `train_jury.py` against the identical dataset splits to independently verify, and got 100.0%/100.0% val/test accuracy instead, since `train_jury.py` pins no random seed and PPO training is stochastic. Both results are real, independently-executed training runs, not the same number restated or one fabricated — but the exact decimal is not reproducible run-to-run, only the qualitative result (near-perfect accuracy, 0% false-negative rate) is stable across the two runs observed. Updated `marl/README.md`, the paper's Table 6, and its abstract to report the checkpoint's actual current, freshly-verified numbers (100.0%/100.0%) with an explicit stochasticity note, rather than leaving a stale, no-longer-reproducible figure in place; the newly-trained checkpoint is also what's now on disk at `marl/checkpoints/jury_ppo`, verified against the existing `test_jury_rl.py`/`test_jje.py`/`test_jje_consensus.py` suites (17/17 passing). [Claude Code]

### Fixed
- **Audit-chain `VerifyChain()` never checked `rows.Err()` after its scan loop (BUG-005)** (`kb-control-plane/internal/audit/audit.go`, `audit_test.go`): asked to fix the open bugs a LaTeX paper draft (`latex/paper/main.tex`) had listed from an earlier security-audit pass. Before fixing anything, re-verified every one of that draft's ~15 claimed-open findings directly against current source rather than trusting the write-up — found the great majority (the unauthenticated HTTP API, the telemetry-reset-containment bug, the LSM fail-open ordering, the custom SSH server's throttling gap, three JJE consensus defects, kbctl's claimed `os.Exit(1)` cleanup bypass, the dashboard's hardcoded API host) were already fixed in team history predating this repo's own audit text, which had simply never been re-checked before being repeated across revisions. One claim was confirmed genuinely still open: `VerifyChain()`'s row-iteration loop exits without calling `rows.Err()` — `sql.Rows.Next()` returns `false` on both ordinary exhaustion and a genuine driver/cursor-level error, with the real error only available via `rows.Err()` afterward, so an interrupted scan was silently indistinguishable from a clean, complete one and got reported as `chain_intact: true` with a short row count instead of surfacing the error — a false-confidence bug, not a false-alarm one. Fixed by checking `rows.Err()` immediately after the loop. New regression test (`TestVerifyChainSurfacesCursorErrorInsteadOfFalseSuccess`) uses a small fault-injecting `database/sql/driver` wrapper (register a driver that fails the *second* `Next()` call, not the query itself) to reproduce the exact failure mode — the existing scan-error test covered a different, already-correctly-handled path (a bad `Scan()` mid-row) and didn't exercise this one. Full `kb-control-plane` suite: 128/128 passing (was 127; one new test). [Claude Code]
- **Stale, self-contradicting comment in `kb-core`'s exit handler** (`kb-core/ebpf/kbd_sensor.bpf.c`): found while re-verifying the paper's kb-core defect claims — a comment asserted `kb_syscall_totals`/`kb_cred_prev` "were never cleaned up here" directly above the two `bpf_map_delete_elem()` calls that already clean them up (BUG-008 was fixed at some earlier point without updating the comment describing the pre-fix state). Corrected the comment to describe current behavior, and clarified why a third similarly-named map, `kb_syscall_counts`, is deliberately *not* cleaned the same way (composite `pid<<32|syscall_nr` key, can't be targeted by a single-PID delete; uses `BPF_MAP_TYPE_LRU_HASH` instead, which ages out entries under normal churn — a different, adequate mitigation, not an oversight). No behavior change; `kb-core`'s full `make`, including eBPF compilation, verified clean before and after. [Claude Code]

### Documentation
- **`latex/paper/main.tex` security-audit section corrected to match re-verified current source, not the state it was originally drafted against**: the paper's own earlier revision had drifted from the codebase — most of its "open defects" were fixed by other contributors well before this revision's audit text was written, and had simply never been re-checked. Corrected throughout: kb-control-plane's HTTP API (bearer-token auth + CORS origin allowlist, not unauthenticated/wildcard), the telemetry-containment-reset claim (`UpsertProcessState` already preserves containment across updates), kb-core's LSM fail-open-ordering and PID-map-exhaustion claims (both already resolved, see above), kb-op's `kb-tui` section (the custom Go SSH server it described as still in place, with OpenSSH migration as a "future" decision, was actually fully migrated and deleted well before this — `sshd@kb-operator.service` with `ForceCommand` now owns that listener entirely, per `docs/architecture/boot_sequence_spec.md` and `control-plane-catalog.md` §2.11 STATUS: DONE), kbctl's claimed `os.Exit(1)` bypass (not present in current source), and kb-dashboard's claimed hardcoded API host (resolves from `window.location.hostname`, configurable via `VITE_KB_API_BASE`, already token-authenticated). The one claim that held up (audit-chain `rows.Err()`) is now marked fixed in this revision. Added an explicit methodological note in the paper itself: a security audit's claims are only as current as the last time they were checked against real source, and this paper's own history is now the worked example of why that check can't be skipped on a revision. Also converted the paper to a two-column layout, rebuilt both architecture diagrams (the original single-column versions had overlapping/crowded labels), and added seven `algobox`-formatted pseudocode blocks (CPM/CWP classification, token-bucket rate limiting, audit-chain log/verify, JJE consensus round, militia escalation sequencing, courthouse verdict) transcribed from a dedicated re-read of the actual C/Go/Python source rather than paraphrased from memory. [Claude Code]

### Added
- **kb-aads: SwarmRegistry + JJE courthouse oversight mechanism — closes the "prerequisite that doesn't exist yet" gap for JJE's sub-agent authority** (`kb-aads/swarm/registry.py`, `agents/base_agent.py`, `swarm/orchestrator.py`, `agents/militia.py`, `consensus/jje.py`, `docs/development/control-aads/aads-intelligence-roadmap.md`): asked to build JJE courthouse oversight (agent health/revocation ladder) and `SwarmMemory` together; scoped down to courthouse-only after confirming `SwarmMemory`'s own design doc (`ray-shared-mutable-state.md`) is flagged "Proposal — needs review... for Karthik to confirm, amend, or reject" with unresolved open questions (e.g. whether mid-round vote visibility is even needed) — building it would mean guessing past an explicit stakeholder-review gate, so it stays unimplemented.
  - **`SwarmRegistry`** (`swarm/registry.py`): the actor-lookup prerequisite the roadmap's JJE section explicitly called out as missing (`RaySwarmOrchestrator.agents` is a plain-object dict a remote `JudgeAgent` can't reach). Follows the roadmap's own "Registry ownership" proposal with one deliberate simplification: registration happens at each **spawn site** (`orchestrator.spawn_agent`, `JudgeAgent.coordinate_consensus`'s per-round Jury pool, `MilitiaSquadLeadAgent.command_squad`'s per-stage members) instead of each agent self-registering inside its own `start()` — same coverage of dynamically-spawned actors, without touching `BaseAgent` or any existing agent subclass's constructor.
  - **JJE courthouse** (`consensus/jje.py`'s `JudgeAgent`): `assess_severity(agent_id)` classifies a registered agent as `"healthy"`, `"restart"`, or `"terminate"` (only if already unregistered), using two real, computable signals — `AgentState` gained an `error_count` field (incremented in `BaseAgent.process_messages`'s existing exception handler, previously uncounted) and a genuine two-read liveness check (uptime must advance across a wait window). `enforce_verdict(agent_id, tier)` mechanically applies the roadmap's graduated response: `"restart"` (stop then re-`start()` the same actor), `"revoke"` (stop + mark revoked in the registry, pulled from active rotation without killing the process), `"terminate"` (`ray.kill` + unregister).
  - **Left honestly incomplete, not guessed at** (documented in both the code and the roadmap doc): `assess_severity` never automatically returns `"revoke"` or `"terminate"` — this codebase has no peer-baseline output-drift detector or authorization-boundary-violation detector anywhere, so those tiers need a human or a future detector to call `enforce_verdict` directly. The error-count/liveness-window thresholds are uncalibrated placeholders (same posture as `HealerAgent`'s `DEFAULT_SAFE_SCORE_THRESHOLD`) — the roadmap's own "Severity thresholds" section already says real cutoffs need rogue-agent-behavior data that doesn't exist yet. The roadmap's "Quorum vs. unilateral" gating (route restart/revoke through Judge→Jury before acting) is also not built: `JuryAgent.evaluate_and_vote` only evaluates CONTAIN/ALLOW threat alerts, and forcing it to answer "is this sub-agent rogue" would mean feeding a fabricated confidence score to force a fixed outcome, not a real vote.
  - New `tests/test_swarm_registry.py` (5 tests) and `test_jje_courthouse.py` (11 tests) — full suite now 68/68. Verified live: a full `start_swarm()` boot shows every spawned agent (including `signal_relay`) present in the registry snapshot, and `assess_severity` correctly reports `"healthy"` for a genuinely running Patroller actor. [Claude Code]
- **kb-aads: Containment militia and signal relay roles implemented — closes the last two "designed but not built" agent roles from `aads-intelligence-roadmap.md`'s per-agent table** (`kb-aads/agents/{militia,signal_relay,base_agent}.py`, `swarm/orchestrator.py`, `config/agents.yaml`, `marl/README.md`, `docs/development/control-aads/aads-intelligence-roadmap.md`):
  - **Containment militia** (`agents/militia.py`): `MilitiaSquadLeadAgent.command_squad(pid, target_level, reason)` spawns one `MilitiaSquadMemberAgent` per stage (`CGROUP`/`SECCOMP`/`NAMESPACE`/`TERMINATE`, matching `ContainmentAgent`'s `LEVEL_NAMES` order) dynamically per incident — mirroring `JudgeAgent`'s existing per-round Jury-pool spawn pattern — and executes them sequentially via `await` so no two stages can race on the same PID (the exact correctness concern the roadmap's original militia row raised: "N conflicting enforcement actions on one process" if members decided independently). Each squad member is genuinely mechanical, no judgment: it calls `ControlPlaneClient.set_containment` (a real gRPC method that existed in `comms/grpc_client.py` since an earlier session but had zero live callers until now) for exactly the one stage it was built for. Escalation halts on first stage failure — no retry/recovery policy defined, a real stated gap, not a silent swallow. `RaySwarmOrchestrator.spawn_militia_squad(pid, target_level, reason)` is the on-demand entry point; **not** auto-wired into `JudgeAgent.coordinate_consensus` (that needs Judge to also learn a target containment level from `ContainmentAgent.decide()`, a separate integration decision explicitly left open, not decided here).
  - **Signal relays** (`agents/signal_relay.py`): `SignalRelayAgent` round-robins a `{"type": "RELAY", "route": ..., "payload": ...}` message to a named destination pool (`routes: dict[str, list[ActorHandle]]`), mirroring `PatrollerAgent`'s existing round-robin-over-a-pool dispatch shape — pure mechanical throughput, no model, ever (per the roadmap's explicit "a model anywhere in this path... would undermine the whole reason a relay layer exists"). Wired into `swarm/orchestrator.py`'s `start_swarm` with one concrete route today (hunter pool, `config/agents.yaml`'s new `swarm.signal_relay` count) — other agent-pairs (Hunter→Judge, Judge→Jury, Judge→militia squad lead) are **not** rerouted through relays; Patroller/Hunter/Judge still call each other directly, per the roadmap's own still-open "which pairs route through a relay vs. direct call" question.
  - `AgentRole` gained `MILITIA_LEAD`/`MILITIA_MEMBER`/`SIGNAL_RELAY`. New `tests/test_militia.py` (6 tests: stage-sequencing at each target level, halt-on-failure, message routing, invalid-level rejection) and `tests/test_signal_relay.py` (5 tests: forward, round-robin, unknown-route no-op, message-type filtering, no-routes no-op) — all pass alongside the full existing suite (52/52). Verified live: a full `start_swarm()` boot shows the signal-relay agent active with its hunter route wired, and `spawn_militia_squad` against a PID with no real control-plane socket running fails cleanly at the first stage (`UNAVAILABLE` from gRPC) rather than crashing, confirming the halt-on-failure path works under a real (if disconnected) `ControlPlaneClient`.
  - Docs updated to match: `aads-intelligence-roadmap.md`'s "neither role exists yet" line (correction note added above it, original scoping questions kept as historical record per this repo's convention) and `marl/README.md`'s Jury/Healer/Patroller update section gained a matching note. [Claude Code]
- **kb-aads: Jury RL-trained, Patroller and Healer given real logic — closes the Jury/Healer/Patroller-stubs gap** (`kb-aads/marl/env.py`, `train_jury.py`, `consensus/jje.py`, `agents/patroller.py`, `agents/healer.py`, `swarm/orchestrator.py`, `main.py`, `config/agents.yaml`, `marl/README.md`, `docs/development/control-aads/aads-intelligence-roadmap.md`):
  - **Jury, actually trained**: new `JuryEnv` (`marl/env.py`) reuses Containment's exact labeled dataset (`scripts/dataset/output/{train,val,test}.csv`) — `target_containment > 0` collapses to CONTAIN, `== 0` to ALLOW, since "should this be contained at all?" is a strict coarsening of Containment's question, not a different one needing new data. `train_jury.py` mirrors `train_containment.py`'s exact RLlib PPO setup; reward follows `marl/README.md`'s pre-existing Reward Signals table (TP +1.0/FP -0.5/TN +0.1/FN -1.0) verbatim. **Real training run, executed locally (CPU, no GPU needed — small tabular env, same as Containment)**: 99.8% val accuracy, 99.4% held-out test accuracy, 100% CONTAIN recall, 0% false-negative rate (908 test samples). Checkpoint at `marl/checkpoints/jury_ppo`. `JuryAgent.evaluate_and_vote` now loads it (`_load_jury_policy`, same inference-only pattern as `ContainmentAgent._load_policy`) and runs real inference when `alert_payload` carries a full obs vector (`score`/`zone`/`uid_is_root`/`score_delta`/`event_type`) — falls back to the original fixed-threshold `decide_vote` when no checkpoint exists OR the caller only sent a bare `confidence` float, which is what every existing caller/test does today, so nothing broke (all 14 pre-existing `test_jje*.py` tests still pass unmodified). New `tests/test_jury_rl.py` verifies the trained policy actually distinguishes a clear-attack obs (CONTAIN) from a clear-benign obs (ALLOW) through a real Ray actor, plus the fallback path.
  - **Patroller, real rule-based escalation** (was: a static "Monitoring baseline" string and an unused `monitored_pids` set): now tracks per-PID score, dispatches to a Hunter — round-robin across a configured pool — the moment a PID first crosses `suspicious_threshold` (default 40.0, `config/agents.yaml`'s new `patroller.suspicious_threshold`), re-arms per-PID once score drops back below threshold. Uses the exact `{"type": "ZONE_TRANSITION", "to_zone": "SUSPICIOUS", ...}` message shape `HunterAgent.handle_message` already listens for — no Hunter-side changes needed. This is correctly rule-based, not RL — `aads-intelligence-roadmap.md`'s own per-agent table always scoped Patroller as "rule-based / lightweight statistical scoring, not RL or LLM." Required wiring Hunter actor references into Patroller at construction (`swarm/orchestrator.py`'s `start_swarm` now spawns Hunters before Patrollers so their pool can be injected, mirroring how Judge already gets `executor_ref`) — `spawn_agent` gained `**extra_kwargs` passthrough for this. New `tests/test_patroller.py` (6 tests) covers threshold crossing, re-arming, round-robin dispatch, and the no-hunter-configured degrade-not-crash case.
  - **Healer, real rule-based restore logic — deliberately NOT RL-trained, and that's a stated real gap, not an oversight**: `agents/healer.py`'s docstring explains why training is blocked — Healer's question ("was un-containing this PID actually safe?") needs restore-OUTCOME ground truth that doesn't exist anywhere in this repo (Phase 0's dataset has attack-category labels only; the Phase 6 analyst-feedback loop that would produce restore-outcome labels is still undesigned per `marl/README.md`'s own Training Pipeline section). Training on anything else would mean fabricating reward labels — worse than an honest rule. Implemented instead: `HealerAgent.decide` requires zone==SAFE AND score below a safe threshold (20.0, well under `config/policy.yaml`'s 40.0 suspicious floor) AND a minimum containment duration (60 ticks) — ALL three, with any missing/ambiguous field defaulting to the least permissive read (never accidentally RESTORE). New `tests/test_healer.py` (8 tests) covers each gating condition independently plus the safe-default-on-missing-data case.
  - Verified end-to-end, not just unit-tested: a real `RaySwarmOrchestrator.start_swarm()` run (8 agents: executor/judge/2 hunters/2 patrollers/healer/containment) confirmed Patroller's configured threshold shows in its live status and Containment's trained checkpoint actually loads (`model_loaded`/"policy loaded" in status) in the same swarm boot.
  - Docs corrected to match: `marl/README.md`'s "Jury and Healer are still...stubs" line (now describes what's actually built and, for Healer, exactly why RL remains open) and `aads-intelligence-roadmap.md`'s 2026-07-24 stub inventory (correction note added, original kept as historical record per this repo's established convention — not rewritten in place). [Claude Code]
- **CWP §4.3/§11.4: signed local workload policy** (`kb-control-plane/internal/ipc/cwp.go`, `cwp_keys.go`, `cmd/kbd/main.go`, `kb-op/kbctl/cmd_workload_sign.go`, `docs/features/CWP.md`): closes the "signed policy" half of the two remaining documented CWP gaps. `SignWorkloadsFile`/`VerifyWorkloadsSignature` implement Ed25519 signing over `workloads.yaml`'s `critical_workloads` + `policy_metadata` content (JSON-marshaled through a fixed-field struct for deterministic byte layout, excluding the signature field itself). New `kbd --workloads-pubkey <path>` flag makes verification mandatory when set — a missing/invalid/tampered signature is rejected outright and the previous known-good registry is kept (never a partially-trusted load), matching CWP.md §4.1's "retains previous known-good snapshot" principle. `kbctl workload keygen`/`sign` give operators an actual signing workflow (kbctl is a separate Go module from kb-control-plane, so its signing logic is an intentionally small, self-contained mirror of cwp.go's — Go's internal-package rule blocks a cross-module import regardless of the module's own `replace` directive, confirmed by testing the import directly). **Explicitly not implemented**: §4.3's networked "Central Policy Store" push/pull — no fleet-management server exists anywhere in this codebase for any subsystem; inventing one (transport, auth, discovery) is a separate infrastructure decision, not a natural extension of this change. Verified end-to-end: `kbctl workload keygen` + `sign` produces a file `kbd`'s independent Go verification path accepts, and a tampered/wrong-key/missing-signature file is rejected (new tests in `cwp_test.go`). [Claude Code]
- **CWP §9: severity-escalated alerting** (`kb-control-plane/internal/controlplane/severity.go`, `controlplane.go`'s `OnZoneTransition`, `http.go`, `proto/kb.proto`): the other remaining documented CWP gap. `Alert` proto gained `protected_workload`/`owner_team`/`justification`/`policy_id` fields (regenerated via `make proto`). `classifySeverity` buckets a BORDERLANDS-entry alert's raw score into LOW/MEDIUM/HIGH/CRITICAL (previously hardcoded to always "CRITICAL"); `findCWPMatch` escalates one tier (capped at CRITICAL) when the alerting process matches a CWP-registered workload, logging a `[CWP] Protected Workload Alert — ESCALATED` line matching CWP.md §9.3's example format and a `CWP_ALERT_ESCALATED` audit entry. `CWPWorkloadEntry` gained `PolicyID`/`OwnerTeam`/`Justification` (Go-side only, load-order-assigned — the wire frame to the sensor is unaffected, still path+tier+hash only). **Known, documented limitation**: the Go-side match is by executable *basename* (`comm`), not the canonical resolved path §5.2/§11.2 require, since kbd never receives a process's full path over the wire — the real path-resolved, spoofing-resistant match still happens kernel-side in `cwp_classify()` for the actual containment-exemption decision; this basename match only affects alert severity/routing, not enforcement. The historical REST alert-reconstruction endpoint (`http.go`, built from `zone_transitions` rows) now uses `classifySeverity` too for consistency, but can't reconstruct CWP escalation after the fact (not persisted in that table) — documented inline rather than silently left wrong. [Claude Code]
- **Exfiltration/beaconing detection** (`kb-core/userspace/bridge/kb_bridge.{h,c}`, `kbd_sensor.c`, `kb-control-plane/internal/ipc/wire.go`, new `internal/detection/exfil.go`, `controlplane.go`'s new `OnNetFlow`, `internal/policy/policy.go`): implements `docs/development/control-aads/dev-exfiltration-detection.md`'s §3.A (Shannon timing entropy) and §3.B (CUSUM) blueprint, previously pure design doc with zero code. New wire message `KB_WIRE_MSG_NET_FLOW` (msg_type=9, `{pid, daddr, dport, ts_ns}`, 22 bytes) sent from `kbd_sensor.c`'s existing userspace `KB_EVT_NETWORK_CONNECT` handler — **no eBPF/kernel-side change**, per the doc's own "out-of-band, to avoid Ring 0 overhead" recommendation; `kb-core`'s full `make` (including eBPF compilation) verified clean. `internal/detection.NetFlowWindow`/`CalculateTimingEntropy` are a near-verbatim port of the doc's own Go example. An `EXFIL_BEACON_SUSPECTED` alert fires only when BOTH entropy is below threshold (1.2, the doc's own example value) AND CUSUM has crossed — combining both cuts false positives either alone would produce — debounced to one alert per (pid,daddr,dport) flow per 5 minutes (without this, a sustained beacon would re-alert/re-audit-log on every single subsequent connection forever; caught by an integration test before shipping). Auto-NAMESPACE containment is available but opt-in per-comm via new `policy.yaml` key `auto_namespace_on_exfil_beacon` (default false — a statistical heuristic has real false-positive risk, same posture as `auto_terminate`). CWP escalation (above) applies to these alerts too. **Fidelity limitation, stated in the code and doc**: CUSUM runs over connection-frequency-per-time-bucket, not real byte volume — `connect()` events carry no payload-size field, and adding one needs new `write()`/`sendto()` syscall telemetry, out of scope here. §3.C (autocorrelation) was not implemented — entropy+CUSUM combined already cover the doc's own worked example. [Claude Code]

### Fixed
- **`kbd`'s DEFAULT `--policy` path silently loaded a broken, unimplemented-schema policy file — corrected diagnosis and fixed** (`kb-control-plane/config/policy.yaml`): first reported in this changelog (previous entry, now corrected) with the two files' roles backwards — verified empirically this time by actually running `policy.New` against both real files rather than inspecting them by eye. Ground truth: `internal/policy/policy.go`'s `ProcessPolicy`/`PolicyFile` structs (`comm`/`suspicious`/`borderlands`/`auto_terminate`, `defaults`, `sensitive_paths`) are unambiguous — root `config/policy.yaml` matches that schema and loads correctly (confirmed: 3 policies, `AutoTerminate("postgres")=true`, 1 sensitive path). `kb-control-plane/config/policy.yaml` — the file `cmd/kbd/main.go`'s bare `--policy` default (`config/policy.yaml`, relative to CWD) resolves to when `kbd` is run from inside `kb-control-plane/` per every documented run command, including `docs/development/full-pipeline-run-guide.md`'s own default-flag description — had the old, pre-`internal/policy` schema (`policies: - name: ... match: {comm: ...} allow: {...} deny: {...}`) and a comment self-admitting "the Policy Engine itself... hasn't been implemented yet." `policy.New` doesn't error on this — it just silently produces policies with an empty `Comm` key, so `AutoTerminate`/`SensitivePaths` return nothing beyond the compiled-in floor with zero warning. Any operator running `kbd` without remembering to pass `--policy ../config/policy.yaml` (the documented golden path explicitly overrides the default for exactly this reason, though never says why) got silently no per-process policy enforcement. Fixed by replacing `kb-control-plane/config/policy.yaml`'s content with the real, working schema (now byte-identical in substance to the root copy) — verified both paths now load identically (`AutoTerminate("postgres")=true` from both). [Claude Code]

---

## [Unreleased] - 2026-08-30

### Added
- **CI workflow added — closes BR-001 from `docs/reports/repository-analysis-2026-08-27.md`** ("No automated CI test workflow... regressions can merge without automated validation"; only `.github/workflows/retry-pages-deploy.yml` existed before this). New `.github/workflows/ci.yml`, one job per subsystem, on push/PR to `main`:
  - `kb-control-plane` (Go): `go build`/`go vet`/`go test ./...`.
  - `kb-core` (C/eBPF): installs clang/bpftool/libelf/zlib, builds vendored `libbpf`, runs `make` — **build-only, deliberately**: `tests/test_all_hooks.sh` is an interactive, log-matching driver script that expects an already-running `sudo ./build/kbd_sensor` with BPF LSM enabled, which stock GitHub-hosted runner kernels don't reliably have — not something to gamble on in shared CI infrastructure. A clean build still catches eBPF verifier rejections and userspace compile breaks, which is most of what changes here risk.
  - `kb-checker` and `kb-tui` (Rust): installs `libelf-dev`/`pkg-config`/`protobuf-compiler` (closes BR-003's native-dependency gap and BR-004's missing-`protoc` gap — both crates' `build.rs` use `tonic-build`), `cargo build --release` + `cargo test` (kb-tui has no test suite yet, build-only).
  - `kb-op-go` (matrix: `kbctl`, `kb-mcp`): `go build`/`go vet`/`go test ./...` each.
  - `kb-dashboard`: `npm ci`, `npm run lint` (oxlint), `npm run build`.
  - `kb-aads`: fresh `pip install -r requirements.txt` + `pytest` — closes BR-002's reproducible-bootstrap gap (verified locally against a throwaway venv before committing: 23/23 pass from a clean install, not just the pre-existing dev venv).
  - Every job's commands were run locally against this exact repo state before being committed to the workflow (not just written and assumed correct) — all pass: `kb-control-plane` full Go suite, `kb-checker` (`cargo test`, 1 passed), `kb-tui`/`kbctl`/`kb-mcp` builds, `kb-dashboard` lint+build, `kb-aads` 23/23 from a fresh venv, `kb-core` clean `make`.
  - **Caught after the fact, by the user asking "libbpf isn't pushed to GitHub, no problem?"**: `libbpf/` is deliberately gitignored (third-party dependency), so a fresh CI checkout never has it — the `kb-core` job's original "build if not already built" step would have failed outright on a real GitHub Actions runner, since the directory itself doesn't exist there, not just the compiled `.a`. Fixed to clone it first (`git clone --branch v1.8.0 --depth 1`). Version pin corrected to v1.8.0 to match what's actually vendored in this repo (`libbpf/src/libbpf_version.h` reports 1.8, not the v1.4.0 `scripts/setup/install.sh` referenced) — that local-dev script had the same gap (only warned a human to clone manually, at the wrong version) and got the same fix, now auto-cloning at the correct version. [PardhuVarma]
- **CWP (Critical Workload Protection) closed the loop — Go-side sender built for a kb-core implementation that already existed** (`kb-control-plane/internal/ipc/cwp.go`, `internal/controlplane/{controlplane.go,grpc.go}`, `cmd/kbd/main.go`, `proto/kb.proto`, `kb-op/kbctl/cmd_workload.go`, `config/workloads.yaml`, `docs/features/CWP.md`, `docs/development/core-control/control-plane-catalog.md`): asked to "build CWP" as the next development item; investigation found `docs/features/CWP.md` (Status: Design Specification) and the catalog's "Explicitly Out of Scope" section (Confirmed unimplemented) were both stale — the kernel/sensor side (`protected_workloads_map`, `cwp_classify()` gated after CPM, path/hash identity tiers, the `KB_WIRE_MSG_CWP_WORKLOADS` wire receiver, orphan-reconciliation sweep) was already implemented and verified live in an earlier session (`kb-core/tests/test_cwp.py`), just never had its docs updated. The one genuinely missing piece — no Go-side sender existed, so operators had no way to actually register a protected workload despite the sensor being fully able to enforce one — is now built:
  - `internal/ipc/cwp.go`: `LoadWorkloadsYAML` parses `config/workloads.yaml` (CWP.md §12.1's schema); for `identity_tier: hash` entries with no explicit `expected_hash`, computes SHA-256 locally by reading the file at `path` (kbd runs on the same host as the sensor — simpler than requiring operators to precompute and paste a hex digest). `SendCWPWorkloads` encodes the exact wire frame `apply_cwp_workloads_frame` (kb-core) already parses.
  - Pushed at sensor-connect time (`ipc.Listener.pushConnectTimeFrames`) and, since the sensor polls for CWP updates continuously at runtime rather than only at the connect-time handshake (unlike the rules/sensitive-paths pushes), also live via a new `BroadcastCWPWorkloads` — an operator can register a workload without restarting `kbd` or waiting for a sensor reconnect.
  - New `ReloadWorkloads` gRPC RPC (mirrors `ReloadPolicy`'s shape) and `kbctl workload reload` as its caller. `cmd/kbd/main.go` gained `--workloads` (default `config/workloads.yaml`, matching `--rules`/`--policy`'s pattern; a missing/empty file is never fatal — CWP is opt-in).
  - `config/workloads.yaml` added, empty (`critical_workloads: []`) by default with a documented example, since CWP is opt-in and no existing deployment expects this file.
  - Docs corrected, not just code added: `CWP.md`'s Status line and the catalog's CWP entry both explicitly called out as having been stale, with what was actually already done vs. newly built spelled out so a future session doesn't re-discover the same gap or re-implement kb-core work that already exists.
  - Verified: full `kb-control-plane` test suite passes (13 new tests in `internal/ipc/cwp_test.go` covering wire format, YAML parsing including both identity tiers and local hash computation, and connect-time/live-broadcast wiring; 3 new `TestReloadWorkloads_*` RPC tests in `grpc_test.go`), `kbctl` builds clean (`kbctl workload reload`), `kb-core` still builds clean (unaffected — this pass was Go-only). [PardhuVarma]

### Fixed
- **Dynamic Rule Delivery System actually wired up — was "Status: Completed" in docs, but never called in production** (`kb-control-plane/internal/ipc/{listener.go,rules.go}`, `kb-core/userspace/sensor/kbd_sensor.c` comment, `docs/development/core-control/dynamic-rules.md`, `kb-control-plane/README.md`): `SendRulesPayload` compiled `rules.yaml` into wire format correctly and had its own passing unit tests, but had zero production call sites — editing `rules.yaml` had no effect on a running sensor. `kbd_sensor.c` itself already carried an honest comment documenting this exact gap. Wired `SendRulesPayload` into `ipc.Listener`'s connect-time push (extracted the existing sensitive-paths push into a new `pushConnectTimeFrames` method so both are unit-testable together against a `net.Pipe()`, not just a live socket), added `--rules` to `cmd/kbd/main.go` (default `config/rules.yaml`, matching `--db`/`--policy`'s pattern), and threaded `rulesPath` through `controlplane.New()`.
  - **Caught and fixed a real ordering bug while building this, not after**: `kbd_sensor.c`'s connect-time handshake calls `read_rules_from_bridge()` first, then `read_sensitive_paths_from_bridge()` second — only the second call has a stash-based fallback for "the wrong frame showed up here instead." The first implementation sent sensitive-paths before rules (matching the pre-existing code's variable-declaration order, not the C side's actual read order), which would have silently corrupted the connection's later framing (containment commands are read off the same fd afterward) despite passing every other test — Go-side tests alone couldn't have caught this since both frames would still arrive intact, just in the wrong order for the specific consumer on the other end. Fixed to send rules first; added `TestPushConnectTimeFrames_RulesSentBeforeSensitivePaths` specifically to guard the ordering itself, not just that both frames get sent.
  - Verified: `kb-control-plane` full test suite passes (new `TestPushConnectTimeFrames_*` tests in `internal/ipc/rules_push_test.go`), `kb-core` (`make`) builds clean with the corrected comment at `read_rules_from_bridge()`. [PardhuVarma]

### Changed
- **Three remaining `control-plane-catalog.md` Open Gaps closed (§2.5, §2.7, §2.12), plus two stale-doc fixes found along the way (§2.6, §2.9)**:
  - **§2.5 (`process_exit` wire message) — turned out to already be resolved.** Investigating the sketch fix's own hint ("check whether `ProcessExitMsg`/`OnProcessExit` already covers this") confirmed `KB_WIRE_MSG_PROCESS_EXIT` is sent by `kb-core` and fully handled Go-side (`OnProcessExit` → `store.TerminateProcessState`, which deletes both L1 and L2) — already covered by `TestOnProcessExitFlushesCache` and credited in this file's own history to "Process Exit Lifecycle (kb-control-plane)" [Tejaswini4119]. The catalog section was simply never updated after that landed. Removed the now-inaccurate `// TODO: C side should send a dedicated process_exit wire message type.` comment left in `OnProcessState` (`controlplane.go`) describing a gap that no longer exists, rather than building something already built.
  - **§2.6 (policy hot-reload) and §2.9 (`GetProcessState` NotFound)** — same situation, confirmed already resolved (matching `BUG_REPORT.md`'s RESOLVED-3/RESOLVED-2), catalog markers were just missing. No code changes needed, doc-only sync.
  - **§2.7 (CLI flags) — real work, done.** `cmd/kbd/main.go` gained `--http-addr`/`--grpc-socket` cobra flags with real `--help` text, matching `--db`/`--policy`. `KB_HTTP_BIND`/`KB_GRPC_SOCKET` still work as the flag defaults, so existing deployment scripts are unaffected. `ControlPlane.Start`/`Stop` signatures changed to accept these explicitly (`Start(httpAddr, grpcSocketPath string) error`, `Stop(grpcSocketPath string)`) rather than each re-reading the env var independently — `cmd/kbd/main.go` was the only caller, so this was a clean, non-breaking threading change. Also fixed a related drift risk while in there: `http.go`'s `/api/services` health probe used to re-read `KB_GRPC_SOCKET` on its own rather than using what `Start()` actually bound to, so `--grpc-socket` wouldn't have shown up in the dashboard's own connectivity check — `ControlPlane` now stores the resolved socket path once and `http.go` reads that.
  - **§2.12 (CIS/SSH identity management) — the in-scope piece (step 5, audit tie-in) done; the platform piece (steps 1–4, standing up `step-ca`) explicitly still open, not attempted.** Added a narrowly-scoped `RecordSSHSession(SSHSessionEvent) returns (Empty)` RPC (`proto/kb.proto`, regenerated via `make proto`; `internal/controlplane/grpc.go`) — accepts only `event ∈ {"session_start","session_end"}`, writing one of exactly two fixed audit actions, not an arbitrary-write endpoint. Added `kbctl ssh session-start`/`session-end` (`kb-op/kbctl/cmd_ssh.go`, hidden from `--help`, not interactive) as its caller, and a `ForceCommand` wrapper script (`docs/architecture/boot_sequence_spec.md` §3, `/usr/local/bin/kb-tui-session-wrapper.sh`) that logs session start, runs `kb-tui` as a child (not `exec`'d — has to stay alive to log session-end and propagate the real exit code), then logs session end. Both `kbctl` subcommands treat audit-logging failure as best-effort (stderr warning, exit 0) so an audit outage can never become an operator-access denial. `sshd_config.d/kb-operator.conf`'s `ForceCommand` line updated to target the wrapper instead of `kb-tui` directly. Explicitly not done, and said so in the catalog rather than silently skipped: standing up `step-ca`/OIDC, `TrustedUserCAKeys`/`AuthorizedPrincipalsFile` distribution, and the `operator` system user provisioning — all genuinely platform/infra work outside what this repo's source controls, exactly as §2.12's own "Owner" note originally scoped it. The `identity` field the new RPC/CLI accept is left empty for now (sshd doesn't expose a key fingerprint to `ForceCommand` by any standard mechanism, and there's no cert principal to report without a CA) — wired up as a no-op placeholder ready for when items 1–4 land, not left out of the shape entirely.
  - Verified: full `kb-control-plane` test suite passes (`go test ./...`) including new `TestRecordSSHSession_*` tests; `kbctl` builds clean and `kbctl ssh --help` shows the new subcommands. [PardhuVarma]
- **§2.11 OpenSSH + `ForceCommand` migration implemented (`docs/development/core-control/control-plane-catalog.md` §2.11, STATUS: DONE)**: replaces `kbd`'s in-process, hand-rolled Go SSH server (`kb-control-plane/internal/ssh/` — `server.go`, `auth.go`, `hostkey.go`, `config.go`, `session.go`, `ssh_test.go` — **deleted in full**) with a real, independently-managed `sshd` instance owning port 2222 outright. `docs/architecture/boot_sequence_spec.md` §3 has the actual unit + config: `sshd@kb-operator.service` (its own systemd unit, own host key, `ProtectSystem=strict`/`NoNewPrivileges=true`/etc.) and `/etc/ssh/sshd_config.d/kb-operator.conf` (`Match User operator`, `ForceCommand /usr/local/bin/kb-tui`, `AuthorizedKeysFile /etc/kb/authorized_keys`, `AllowTcpForwarding no`, `X11Forwarding no`). `kbd` and this `sshd` instance now have zero runtime coupling — not a `kbd`-supervised child process, per the catalog's explicit "Decided" rationale (a compromised `kbd` must not be able to manipulate its own auth boundary). `kb-tui` itself needed no changes — it never depended on anything `internal/ssh/` was doing beyond having a PTY and reachable stdin/stdout, which `ForceCommand` provides identically.
  - This directly retires BUG-013 and BUG-014's earlier in-place interim patches (auth-attempt throttling, `KB_SSH_ALLOW_DEV_FALLBACK` opt-in, `KB_TUI_PATH`+fixed-path resolution) — that code and its regression tests are gone, not ported, exactly as those patches' own status notes predicted the day they landed. Auth throttling and dev-mode-fallback risk are now OpenSSH's problem entirely, not this codebase's.
  - Two items from §2.11's own hardening checklist remain open and are called out explicitly rather than silently skipped: certificate-based auth (`TrustedUserCAKeys`, item 2) still depends on §2.12's unstarted CIS/`step-ca` work, so `AuthorizedKeysFile` (flat file, same path the old dev-mode fallback used) stays as an interim auth mechanism; and provisioning the unprivileged `operator` system user + `kb` group membership (item 5) is host-provisioning work outside anything this repo's source controls.
  - `go.mod` cleanup: `go mod tidy` after the deletion dropped the entire `charmbracelet/*` (`ssh`, `wish`, `bubbletea`, `lipgloss`, `keygen`, `log`, `x/*`) + `creack/pty` dependency chain — confirmed via `go build`/`go vet`/`go test ./...` that nothing else in the module used them.
  - Docs synced beyond the catalog itself: `docs/architecture/boot_sequence_spec.md` (kbopt.service section rewritten — also fixed a pre-existing, unrelated-but-directly-adjacent error where it and `developer-commands.md` described `kb-tui`'s build as `go build -o kb-tui cmd/main.go`; `kb-tui` is a Rust/Cargo crate with no `cmd/` directory, corrected to `cargo build --release`), `docs/specifications/operator_interfaces_spec.md` (§C and the interface table corrected — host keys/`authorized_keys`/PTY handling no longer described as living in `kbd`), `kb-control-plane/README.md` (§3 rewritten, `internal/ssh/` removed from the directory-structure list), `BUG_REPORT.md` (BUG-013/BUG-014 status lines updated from "interim mitigation" to fully resolved).
  - Verified: `kb-control-plane` full test suite passes with zero SSH-related tests remaining (there's nothing left in Go to unit-test — correctness of the SSH layer is now OpenSSH's problem, not this repo's). [PardhuVarma]

### Fixed
- **All 14 open findings from `BUG_REPORT.md` fixed in one pass, across all four code subsystems**: verified each against current source first (the report's own methodology), then fixed in place. No documented behavior in `docs/` conflicted with any of these — all were code-level defects, not spec/implementation disagreements.
  - **BUG-001 — unauthenticated, wildcard-CORS HTTP API (`kb-control-plane/internal/controlplane/{controlplane.go,http.go}`)**: `kbd`'s HTTP API now binds loopback-only (`127.0.0.1:8080`) by default — remote access requires explicitly setting `KB_HTTP_BIND`. `/api/isolate` and `/api/restore` now require a `KB_HTTP_API_TOKEN` bearer token (fails closed — refuses all requests if unset, rather than silently allowing them). Wildcard `Access-Control-Allow-Origin: *` replaced with an explicit allowlist (`KB_HTTP_CORS_ORIGIN`, defaulting to the Vite dev server's origins). Dashboard (`kb-op/kb-dashboard/src/App.tsx`) updated to send the token via `VITE_KB_API_TOKEN`.
  - **BUG-002 — `UpsertProcessState` silently reset containment to `NONE` (`kb-control-plane/internal/store/process.go`)**: now loads the existing `Containment` value and carries it forward on every telemetry-driven upsert, matching the load-then-mutate pattern already used by `SetContainment`/`SetZone`.
  - **BUG-003 — JJE Jury's CONTAIN vote was dead code (`kb-aads/consensus/jje.py`)**: threshold corrected from `> 75.0` to `> 0.75` to match the 0.0–1.0 confidence scale used everywhere else in the system.
  - **BUG-004 — `kb_lsm_file_open` failed open before the containment check ran (`kb-core/ebpf/kbd_sensor.bpf.c`)**: the level-≥3 "block all file opens" check now runs *before* `bpf_d_path()` is attempted, so a path-resolution failure can no longer bypass full-quarantine blocking.
  - **BUG-005 — `VerifyChain()` discarded the SQL scan error (`kb-control-plane/internal/audit/audit.go`)**: `rows.Scan()`'s error is now checked and returned instead of silently hashing zero-valued fields and reporting a false tamper verdict.
  - **BUG-006 — Jury pool size hardcoded, ignoring `config/agents.yaml` (`kb-aads/consensus/jje.py`, `swarm/orchestrator.py`, `main.py`)**: `jury.pool_size` is now read from config and threaded through `RaySwarmOrchestrator.start_swarm` → `JudgeAgent`.
  - **BUG-007 — no fault tolerance for a crashed juror (`kb-aads/consensus/jje.py`)**: `coordinate_consensus` now awaits jury votes individually (`asyncio.gather(..., return_exceptions=True)`) and proceeds with whichever jurors actually responded, instead of one crashed actor aborting the whole round.
  - **BUG-008 — unbounded per-PID eBPF maps never cleaned up (`kb-core/ebpf/kbd_sensor.bpf.c`)**: `kb_syscall_totals`/`kb_cred_prev` entries are now deleted in `kb_handle_exit`, same as `protected_pids_map`/`protected_workloads_map`. `kb_syscall_counts` (composite `pid<<32|syscall_nr` key, not directly PID-deletable) converted to `BPF_MAP_TYPE_LRU_HASH` so it ages out under normal PID churn instead of filling permanently.
  - **BUG-009 — dashboard's API base hardcoded to port 8080 (`kb-op/kb-dashboard/src/App.tsx`)**: externalized to `VITE_KB_API_BASE` (host was already dynamic via `window.location.hostname`; port/protocol were not).
  - **BUG-010 — `AgentDecision.AuthorizedBy` captured but never validated (`kb-control-plane/internal/controlplane/grpc.go`)**: destructive actions (`TERMINATE`/`NAMESPACE`/`SECCOMP`/`CGROUP`) now require at least one identity in `AuthorizedBy` to match a configured allowlist (`KB_AGENT_ALLOWLIST`, defaulting to this system's known agent roles) before being acted on.
  - **BUG-011 — no protected-PID gate on the Go control-plane side (`kb-control-plane/internal/enforcement/enforce.go`)**: `Enforcer.Contain` now refuses to contain PID 1, `kbd`'s own PID, or a small configurable list of protected process names (`KB_PROTECTED_COMM`) — independent of `kb-core`'s downstream CPM gate. Restore (level `NONE`) remains exempt.
  - **BUG-012 — no input validation or rate limiting on containment RPCs (`kb-control-plane/internal/controlplane/grpc.go`)**: `SetContainment`/`SubmitAgentDecision` now verify the target PID exists in the store before acting, and enforce a per-caller minimum interval (`KB_AGENT_MIN_INTERVAL_MS`, default 200ms).
  - **BUG-013 — SSH listener had no auth-attempt throttling; dev-mode fallback was a silent key downgrade (`kb-control-plane/internal/ssh/{server.go,config.go}`)**: added a per-source-IP failure counter with backoff on public-key auth. Dev-mode's CWD-relative host-key fallback now requires an explicit second opt-in (`KB_SSH_ALLOW_DEV_FALLBACK=true`) beyond `KB_DEV=true` alone.
  - **BUG-014 — `kb-tui` launch path was a hardcoded single-developer absolute path (`kb-control-plane/internal/ssh/session.go`)**: fallback chain collapsed to `KB_TUI_PATH` (override) and one fixed install path (`/usr/local/bin/kb-tui`); fails loudly with a specific message instead of silently trying three guesses.
  - **BUG-015 — `kbctl audit verify` bypassed its own deferred cleanup (`kb-op/kbctl/cmd_audit.go`)**: replaced the direct `os.Exit(1)` on a detected chain break with a returned error, letting `main()`'s existing exit handling run `defer conn.Close()`/`defer cancel()` first, matching every other command in the CLI.
  - Verified: `kb-control-plane` full test suite passes (`go test ./...`), `kb-aads` full pytest suite passes (9/9), `kb-core` (`make`) builds clean with the LSM-hook reordering and map-type change, `kb-op/kbctl` builds clean, `kb-op/kb-dashboard` type-checks clean (`tsc --noEmit`). [PardhuVarma]
- **`BUG_REPORT.md` and `docs/development/core-control/control-plane-catalog.md` synced to reflect the fixes above**: every `BUG_REPORT.md` finding's `Status` line updated in place (left the original section headers/structure intact as a point-in-time record rather than rewriting the report). Matching catalog sections (§2.2, §2.3, §2.4) got `STATUS: DONE` markers in the same style §2.1 already used. **One real discrepancy surfaced doing this**: §2.10 and §2.13 of the catalog explicitly say "N/A — do not assign, superseded by §2.11's OpenSSH+`ForceCommand` migration (decided), don't patch in place" — but BUG-013/BUG-014 above were already patched in place (SSH auth throttling, dev-fallback opt-in, fixed `kb-tui` path) before this catalog was re-read, working only from `BUG_REPORT.md`. Decision (user-confirmed): keep both as interim mitigations narrowing the exposure window until §2.11 actually lands, rather than reverting them — documented explicitly as interim in both `BUG_REPORT.md` and the catalog's §2.10/§2.13, including the note that this code gets deleted, not ported, once §2.11 ships. [PardhuVarma]
- **Regression tests added for every fix above** (`kb-control-plane/internal/{audit,controlplane,enforcement,ssh,store}/*_test.go`, `kb-aads/tests/test_jje.py`, `kb-aads/tests/test_jje_consensus.py`): none of the 14 fixes shipped with dedicated coverage in the same pass, so this closes that gap directly — new `audit_test.go` and `http_test.go` files, extended `enforce_test.go`/`store_test.go`/`grpc_test.go`/`ssh_test.go`. Two production refactors made purely for testability, no behavior change: `kb-aads/consensus/jje.py`'s vote-threshold and quorum-tally logic pulled into standalone functions (`decide_vote`/`quorum_reached`) so they're unit-testable without a Ray cluster, and `JudgeAgent` gained an optional `jury_agent_cls` constructor param (defaults to the real `JuryAgent`) so tests can inject a fake jury without module-level monkeypatching — needed because Ray's `local_mode` was removed upstream, so worker actors run in separate processes and can't see a driver-process monkeypatch. Also had to fix an inert BUG-013 test gap while adding SSH tests: `internal/ssh/config.go`'s new dev-fallback gate had a `flag.Lookup("test.v") == nil` exemption that silently no-opped the entire check under `go test` (which always registers that flag) — removed the exemption and instead set `KB_SSH_ALLOW_DEV_FALLBACK=true` explicitly in `controlplane_test.go`'s `newTestControlPlane` helper, the one place that constructs a real `ssh.Service` in tests; the new `TestLoadConfig_DevModeWithoutSecondOptInFails` test only passes because that exemption is gone. [PardhuVarma]

### Added
- **Comprehensive repository analysis report (`docs/reports/repository-analysis-2026-08-27.md`)**: Added a full cross-subsystem analysis covering repository architecture, current testing posture, measured coverage status (including package-level `kb-control-plane` coverage summary), agentic module security review (`kb-aads`), consolidated test-case inventory, and prioritized bug findings/remediations (CI test automation gap, agentic ingestion wiring gap, dependency/toolchain blockers, and runtime exposure notes). [Tejaswini]
- **NOMAD LaTeX design document, 47 pages (`latex/nomad/main.tex`, `latex/nomad/main.pdf`)**: A complete, illustrated, first-principles walkthrough of the Node Out-of-Band Module for Attestation & Defense — written to be followable by a reader with no prior security-hardware background, built entirely with custom TikZ house-style macros (no external diagram tool) across dozens of figures: architecture diagrams, the PCR measured-boot hash chain, the recovery ladder, cost tables, a full-system sideways diagram, and more. Includes a new §21 "What-if: Boot/Rootkits?" answering the sharpest question the design invites — what if `kb-checker` itself is compromised, not killed, but alive and lying (framed explicitly as a confused-deputy problem, Hardy 1988) — covering the two-TPM resolution (NOMAD's own TPM vs. the host's own already-present TPM 2.0, since a TPM can only attest to the machine it is physically part of), TPM2 PCR-sealing of `kb-checker`'s OOB signing key to the host's measured-boot chain via `TPM2_PolicyPCR`, a memory-corruption-mitigation narrowing stack (ASLR/KASLR, KPTI, stack canaries, Intel CET, W^X, `kexec`/`devmem` lockdown, LKRG in report-only mode), cross-vantage/canary/clock cross-checks, and a three-bucket failure-classification scheme (silence / cryptographic failure / content implausibility) so a fencing event becomes actionable evidence rather than a generic alarm. §21.4.3's "Demo Environment" box documents a `tpm2-tools` walkthrough (PCR 16, one of the TCG-defined resettable "debug" registers) that was run and verified live on real university-lab hardware, not just described. Closes with a References section (32 entries — the project repository first, then the concept literature actually leaned on throughout: the confused deputy problem, TCG TPM 2.0/PCR specs, IMA, ASLR/KASLR, stack canaries, Intel CET, W^X, ROP/JOP, Meltdown/KASLR-defeat, kernel lockdown, LKRG, BMC/IPMI, HSM/FIPS 140-3, eBPF, `tpm2-tools`, `systemd.exec` sandboxing — each cited inline at its first substantive mention rather than left as an undifferentiated reading list) and a repository link on the title page. [PardhuVarma]
- **`docs/features/hardware-ext/nomad-rootkit-resistance.md`**: New markdown doc resolving `nomad-hardware-appliance.md` §14 Open Question 4 (TPM integration point) — the markdown-house-style twin of the LaTeX doc's §21, deliberately written in this directory's established hedged register ("Proposed, not implemented," explicit Open Questions section) rather than the LaTeX doc's confident, no-hedging one. Covers the same two-TPM resolution, PCR-sealing mechanism (with mermaid diagrams and a sequence diagram in place of TikZ), the memory-corruption-narrowing stack, cross-checking/classification scheme, and the same runnable `tpm2-tools` PCR-16 demo, condensed to markdown+mermaid. Honest throughout about what it does and doesn't close — its own §11/§12 track this explicitly, including that PCR-bank selection, the TPM2 policy-session wire mechanics, LKRG deployment ownership, and fleet-correlation ownership all remain undesigned. [PardhuVarma]
- **Node Out-of-Band Module for Attestation & Defense proposal, v0.2 (`docs/features/nomad-hardware-appliance.md`, renamed/superseding `docs/features/kbc-hardware-watchdog.md`)**: Documentation-only proposal (not implemented) for a physically independent hardware appliance — SBC (attestation/policy engine) + STM32 (deterministic safety controller/watchdog, survives an SBC-side Linux crash) + ESP32 (management/comms only, no security decisions) + TPM 2.0 root of trust + relay-based host fencing — that attests and can fence a Kernel Borderlands host from outside its trust boundary, covering the one failure mode software-only same-host watchdogging can't structurally address: full kernel compromise. Reframed from v0.1's "`kb-checker` hardware extension" framing to a standalone future subsystem (working name `kb-hw/`, not yet created) that continues `kb-checker`'s external-verification role rather than being part of it. Updated bill of materials for the 3-processor design (~$105–$215 bench prototype); added a production-appliance concept (enclosure, front-panel LEDs, display) and cluster-supervision direction, both explicitly out of prototype scope; rejected inline-hardware (FPGA/DPU) tier carried over unchanged. Open questions expanded (repo placement/naming, internal SBC↔STM32↔ESP32 protocol, research-contribution claims needing a literature comparison) on top of v0.1's unresolved wire protocol, failure semantics, and TPM integration point. [PardhuVarma]
- **Fabric Management Service proposal (`docs/features/nomad-fms.md`)**: Documentation-only proposal (not implemented) for NOMAD's fleet-orchestration sub-service, expanding `nomad-hardware-appliance.md` §9 from a two-line "future work" stub into a full design direction — Fabric → Colony → Family → Node hierarchy, discovery/registry/membership, policy inheritance across hierarchy levels, health monitoring, event aggregation, cross-host correlation, recovery coordination, and deployment management. Explicitly scoped to come after a working single-host NOMAD prototype, not before. Flags two unresolved risks rather than asserting the design is sound: whether centralizing fleet management on one NOMAD reintroduces the single-point-of-failure problem NOMAD exists to avoid per host, and an apparent conflict between FMS's multi-node reporting channel and NOMAD's "never the host's primary network" principle from the parent doc. [PardhuVarma]
- **NOMAD/FMS design resolution: Two Communication Planes + Local Autonomy Principle (`docs/features/nomad-hardware-appliance.md`, `docs/features/nomad-fms.md`)**: Closed both risks flagged in the FMS proposal above, same day. NOMAD now exposes two independent, never-shared interfaces (`nomad-hardware-appliance.md` §3.1): an **OOB Trust Plane** (SBC's existing serial/dedicated-Ethernet link to the host's `kb-checker` — attestation, heartbeat, recovery) and a **Fabric Management Plane** (ordinary but hardened networking — dedicated VLAN/NIC/isolated switch — carried on the ESP32, §4.3, used only by FMS/dashboards/policy sync). FMS itself is reclassified under a **Local Autonomy Principle** (`nomad-fms.md` §3.1): every node keeps full local protection with zero dependency on FMS, so FMS is operationally important but explicitly not security-critical. Fleet reporting is data-minimized to signed summaries (health/alerts/inventory/version/integrity status) rather than raw telemetry or BPF map access (`nomad-fms.md` §11.1) — signing-key provenance for those summaries is a new open question, tied to the parent doc's still-unresolved TPM integration point. Multi-NOMAD scale direction also decided: no global master, each Colony keeps its own autonomous NOMAD/FMS pair; the actual cross-NOMAD sync protocol remains undesigned. [PardhuVarma]
- **NOMAD §9 rewritten: dedicated-per-host vs. shared-chassis deployment models, capped and costed (`docs/features/nomad-hardware-appliance.md`)**: Answers "does every protected machine need its own NOMAD?" with two named models instead of the earlier single ambiguous cluster diagram — **Model A** (one full NOMAD unit per host, simplest, no shared failure domain) and **Model B** (one shared chassis serving up to 5 co-located hosts, hard-capped for blast-radius/commodity-part reasons, §9.2). Model B's OOB transport corrected mid-design from a naive point-to-point-serial fan-out (doesn't scale) to a dedicated, physically isolated Ethernet segment per chassis — explicitly not a VLAN on the host's own NIC, since that would still be driven by a potentially-compromised host kernel; modeled on the same pattern enterprise BMC/IPMI management networks and managed PDUs already use. Added §4.2.1 explaining why fencing/heartbeat logic sits on a dedicated STM32 rather than the SBC (avoids re-coupling the watchdog to Linux's own failure domain) or the ESP32 (kept out of the trust-critical path due to its larger, network-facing attack surface) — "SBC = brain, ESP32 = mouth, STM32 = spinal reflex." Added full USD/INR cost tables: shared-vs-per-host component split, single/triple/pentagon(5)/30-host worked examples, itemized per-host cost breakdown (STM32/relay/OOB dongle/wiring), and the marginal cost of filling one chassis to its 5-host cap (~$128–$188 / ₹11,140–₹16,360 for hosts 2–5, on top of one $74–$148 fixed chassis cost). New open questions 9–10 flag that the OOB dongle line item is still an approximation and that neither model has an actual built/tested reference design yet. [PardhuVarma]
- **Cost-reduced NOMAD BOM appendix (`docs/features/hardware-ext/nomad-cheap.md`, alt.)**: Documentation-only companion to `nomad-hardware-appliance.md`'s Model B shared-chassis BOM — tags each cost-reduction option (Pi Zero 2 W, bare ESP32/STM32 modules instead of dev-kit boards, shared multi-channel relay board, dropped optional display, generic OOB dongle) as either "free" (no change to NOMAD's threat model) or "trade-off" (discrete TPM → firmware TPM is the one flagged, and explicitly not recommended by default, since it removes the design's hardware-rooted trust guarantee). Brings a Pentagon (5-host) chassis from $234–$383/₹20,360–₹33,320 down to $114–$168/₹9,918–₹14,616 (~$11–$17 per person on a 10-person team) without the TPM trade-off. Notes bulk/wholesale sourcing doesn't apply at 5-unit scale — the savings come from cheaper parts, not order volume. Does not change architecture or `nomad-hardware-appliance.md`'s open questions. [PardhuVarma]
- **fTPM cost-cut rejected in `nomad-cheap.md` §3.7, same day**: Investigated whether swapping the discrete TPM for a firmware TPM (fTPM) was a viable savings, since it looked like the single biggest line item ($20–$40 → $0). Rejected: fTPM is a SoC feature, not a purchasable part — Intel chips have it built in (Intel PTT, free BIOS toggle) but Raspberry Pi does not, and getting one there would need an unproven OP-TEE + TPM2 Trusted-Application port reworking Pi's non-standard boot chain, with uncertain maturity on Pi 5's SoC specifically. Even if built, it would only deliver TrustZone-level logical isolation on the same chip as the SBC, not a discrete chip's physical separation — and the saving involved (~$4–$8/host amortized) doesn't justify either the engineering risk or the weakened guarantee, especially given the self-undermining angle: NOMAD exists specifically because software-rooted trust isn't considered sufficient, so rooting NOMAD's own trust in firmware reintroduces that same weakness at NOMAD's foundation. Verdict: discrete TPM stays in both the reference and cost-reduced BOMs; `nomad-cheap.md` §7's open question on this is now resolved. [PardhuVarma]
- **Containment RL training pipeline (`scripts/dataset/`, `kb-aads/marl/`)**: Built a real dataset pipeline (`collect.py`/`label.py`/`split.py`) using [ADFA-LD](https://www.unsw.adfa.edu.au/unsw-canberra-cyber/cybersecurity/ADFA-IDS-Datasets/) real Linux syscall-sequence traces — a host-based IDS dataset matching kb-core's actual observation surface, chosen after correctly rejecting an initial NSL-KDD (network-flow) attempt as a mismatch for what KB hunts. Maps 5 of KB's 6 attack-lab scenarios onto ADFA-LD categories (`memory_exploit.sh` has no equivalent — stated, uncovered gap), plus real `/proc` telemetry sampled from the dev machine for benign-class diversity. Full report: [`docs/reports/kb-aads/containment-rl-2026-08-02.md`](docs/reports/kb-aads/containment-rl-2026-08-02.md). [Karthik] [PardhuVarma]
- **Fixed `ContainmentEnv` (`kb-aads/marl/env.py`)**: Replaced the `AADSEnv` stub (single-step, placeholder observations, hardcoded reward) with a real Gymnasium env — observation is the actual `ProcessState`/`KBEvent` wire-field shape, reward computed from labeled targets with an over-/under-containment asymmetry. [Karthik] [PardhuVarma]
- **RLlib PPO training via `colab` CLI (`kb-aads/marl/train_containment.py`, `COLAB_CLI_TRAINING.md`, `remote_train_driver.py`)**: Trained Containment's policy remotely on a Colab CPU session (no local GPU on the dev machine). Final held-out test-set result: 84.4% overall accuracy, 97-100% on every real attack category, **0% under-containment rate**. Full training transcript at `kb-aads/marl/session.log`. [Karthik] [PardhuVarma]
- **Live inference wiring (`kb-aads/agents/containment.py`)**: `ContainmentAgent` now loads the trained checkpoint via `RLModule.from_checkpoint` (inference-only) and drives real containment decisions instead of doing nothing — falls back to `NONE` + explicit `model_loaded: False` if no checkpoint exists, never guesses. [Karthik] [PardhuVarma]
- **Live pipeline demo (`kb-aads/demo/live_containment_monitor.py`)**: Long-running driver that streams real `KBEvent`s from a live `kb-control-plane`, fetches each PID's real `ProcessState`, and feeds it to a persistent, trained `ContainmentAgent` actor — verified against the actual running `kb-core` sensor + `kb-control-plane` on this machine, not synthetic data. [Karthik] [PardhuVarma]
- **base ppr draft (ongoing work)** (`latex/paper/`): arXiv-style LaTeX draft covering all five subsystems (architecture, implementation history, defects, current status), with the Containment RL work as one subsection rather than the whole paper. Built on the `kourgeorge/arxiv-style` template. [PardhuVarma]
- **Compilation hardening + BPF map hardening + systemd sandboxing for kb-core (`kb-core/Makefile`, `kb-core/ebpf/kbd_sensor.bpf.c`, `kb-core/userspace/sensor/kbd_sensor.c`, `docs/architecture/boot_sequence_spec.md`)**: defense-in-depth against memory-corruption exploitation of `kbd_sensor`'s pure-C userspace daemon turning it into a confused deputy, applied carefully to avoid breaking the actively-built binary or CPM/CWP's live wire-protocol pushes.
  - `Makefile`: added `-D_FORTIFY_SOURCE=3 -fstack-protector-strong -fstack-clash-protection -fPIE -O2` / `-pie -Wl,-z,relro,-z,now` to every userspace C binary (all 6 collectors + `kbd_sensor`) — previously zero hardening flags existed (`-g -Wall` only). Verified with a full `make clean && make`: builds clean, and `readelf`/`nm` confirm PIE + full RELRO (`BIND_NOW`) + `__stack_chk_fail` landed in the output binary.
  - `kbd_sensor.bpf.c`: added `BPF_F_RDONLY_PROG` to `contained_pids_map` and `protected_exec_paths_map` — verified both are only ever read by kernel-side eBPF code and written by userspace (at startup and continuously at runtime via wire pushes); the flag blocks any future in-kernel write path without affecting the existing userspace writes.
  - `kbd_sensor.c` `main()`: added `bpf_map_freeze()` on `kb_sensitive_paths` only, placed *after* `read_sensitive_paths_from_bridge()`'s one-shot startup merge and before BPF attach (freezing any earlier, per the naive reading of the standard freeze-after-load pattern, would have rejected that legitimate startup write). **Correction caught during implementation**: `protected_workload_paths_map` was initially also flagged as a freeze candidate by research, but turned out to be written continuously at runtime by `apply_cwp_workloads_frame()` (CWP's live operator-pushed workload registration, called every poll iteration) — freezing it would have silently broken that feature. Left unfrozen after re-verifying against the actual write call sites, not just the initial research pass.
  - `docs/architecture/boot_sequence_spec.md`: added systemd hardening directives to all 5 documented unit files, split by risk. Conservative-only (`NoNewPrivileges=true`, `PrivateTmp=true`, `ProtectHome=true`, `MemoryDenyWriteExecute=true`, no `CapabilityBoundingSet=`/`ProtectSystem=strict`) on `kbd.service` and `kb-checker.service` — the latter's Tampering Containment Protocol shells out to `pkill -9`/`rm -rf /sys/fs/bpf/*`/`iptables -P ... DROP` as a last-resort lockdown with no privilege-dropping code anywhere in `kb-checker` today, and that path is only exercised during a real attack, not routine tests, so a wrong capability enumeration would fail silently until it matters most. Full hardening (adds `ProtectSystem=strict` + `ReadWritePaths=/run/kb` + `CapabilityBoundingSet=` empty) on `kbagents.service`/`kbopd.service`/`kbopt.service` — safe since none are deployed yet and their real runtime needs are unprivileged (UDS/gRPC client + unprivileged-port listener). [PardhuVarma]

### Changed
- **OAN (Out-of-Band Attestation Node) renamed to NOMAD (Node Out-of-Band Module for Attestation & Defense), repo-wide**: Pure rename, no architecture/design change. Covers every prose, heading, label, diagram-node, and code-comment mention across the LaTeX doc and the five `docs/features/hardware-ext/` markdown docs, plus their cross-references in `CHANGELOG.md` and `docs/features/README.md`. File/directory renames: `docs/features/hardware-ext/oan-hardware-appliance.md` → `nomad-hardware-appliance.md`, `oan-fms.md` → `nomad-fms.md`, `oan-cheap.md` → `nomad-cheap.md`, `oan-expense.md` → `nomad-expense.md`, `oan-rootkit-resistance.md` → `nomad-rootkit-resistance.md`, `latex/oan/` → `latex/nomad/`. The still-unresolved repo-placement open question's working-name example was updated too (`kb-oan/` → `kb-nomad/`, `nomad-hardware-appliance.md` §14 item 1) — it remains a placeholder, not a decision. LaTeX title page redesigned to lead with the short name (`NOMAD` as the large 34pt title, the expansion as the `\Large` subtitle below it — the reverse of the old OAN layout, since "NOMAD" reads better as the primary mark than the acronym did) and recompiled clean (50 pages, no new overfull/undefined warnings); internal TikZ style names and node IDs (`oanblock`→`nomadblock`, `oanbox`→`nomadbox`, the `sec:what-is-oan` label, mermaid node IDs `OAN1`–`OAN3`) renamed to match for consistency, even though these aren't reader-visible. [PardhuVarma]
- **`kb-aads/marl/checkpoints/containment_ppo/` un-ignored and committed (`.gitignore`, `kb-aads/marl/checkpoints/`)**: Initially gitignored as a regenerable binary build artifact (same treatment as `kb-core/build/`, Rust `target/`, etc.). Reversed — each checkpoint is confined to its own subdirectory and this one is only ~1.8MB, so committing it keeps the repo immediately runnable (clone and go) without requiring a retrain first. Re-evaluate if a future checkpoint (Jury/Healer, or a larger model) is significantly bigger. [Karthik]

### Fixed
- **`kb-checker` and `kbd` missing from CPM's protected-process floor (`kb-core/userspace/sensor/kbd_sensor.c`, `docs/features/CPM.md`, `docs/features/cpm-implementation.md`)**: CPM.md's protected list was `kb-sensor`, `kb-agent`, `kbctl`, `policy-engine`, `dashboard` — `kb-checker` wasn't on it, so a malfunctioning or manipulated detection engine could get `kb-checker` itself recommended for containment through KB's own pipeline with nothing to stop it. Added `/usr/local/bin/kb-checker` and `/usr/local/bin/kbd` (documented install paths per `kb-checker.service`/`kbd.service`'s `ExecStart=` lines in `docs/architecture/boot_sequence_spec.md`) to `populate_protected_exec_paths()`'s compiled-in floor, and added `kb-checker` to CPM.md's protected-component list (§3.3, §5.2, §7.1). Protecting `kbd` also covers CPM.md's `policy-engine`, which runs as an internal Go package inside `kbd` (`kb-control-plane/internal/policy/`), not as a separate binary — nothing distinct to add for it. Investigated the remaining three names and found none currently addable without guessing: `kbctl` is a one-shot CLI with no systemd unit or documented install path; `kb-agent` (`kbd-agent.service`, the Ray Swarm node agent) is named in the boot-sequence narrative but its `ExecStart=` was never written into `boot_sequence_spec.md`'s unit-file section; `dashboard` (`kb-op/kb-dashboard`) is a Node/Vite dev-server with no systemd unit and isn't mentioned in that spec at all. All three left as explicit, documented gaps rather than guessed paths. [PardhuVarma]
- **Renamed `kbd-agent.service` → `kbagents.service` and wrote its unit file (`docs/architecture/boot_sequence_spec.md`, `docs/development/core-control/kba_uds_binding_spec.md`, `docs/features/cpm-implementation.md`)**: the Ray Swarm node agent was previously named only in the boot-sequence narrative and dependency diagrams, with no `[Service]`/`ExecStart=` block, unlike `kbd.service`/`kb-checker.service`. Renamed per direction and added the missing unit file (`After=`/`Requires=kb-checker.service`, matching the existing dependency-chain prose), then added `/usr/local/bin/kbagents` to CPM's compiled-in floor (`kb-core/userspace/sensor/kbd_sensor.c`) alongside `kb-checker`/`kbd`. **Caveat, called out in both the code comment and `cpm-implementation.md`:** kb-aads (`kb-aads/main.py`) has no compiled-binary build/install step in this repo today — it runs as `python3 main.py`, not a packaged binary — so `/usr/local/bin/kbagents` documents a packaging convention/decision, not an existing build artifact. The unit file and CPM floor entry are correct and harmless now (nothing execs from that path yet) and become load-bearing once a wrapper/packaging step is actually built; tracked as explicit follow-up. [PardhuVarma]
- **Wrote `kbopd.service` and `kbopt.service` unit files and added both to CPM's protected floor, in advance of a real deployment (`docs/architecture/boot_sequence_spec.md`, `kb-core/userspace/sensor/kbd_sensor.c`, `docs/features/cpm-implementation.md`)**: "dashboard" in CPM.md's list was clarified to mean both `kb-op` operator interfaces — the web dashboard (`kb-dashboard`) and the SSH terminal console (`kb-tui`), not just the former. Reasoned that even though their blast radius (operator visibility) is smaller than `kbd`/`kb-checker`'s (enforcement/safety), CPM.md §3.3's own rationale — "an attacker or false positive could blind the platform at the exact moment visibility matters most" — applies just as much to the human-facing consoles during an incident, so both are worth protecting once they have somewhere to attach to. Added `kbopd.service` (dashboard) and `kbopt.service` (terminal console, an actual SSH-server daemon on port 2222 — not a per-session tool), both `After=`/`Requires=kbd.service` since they consume `kbd`'s API and aren't gated by `kb-checker` the way `kbagents` is, plus `/usr/local/bin/kbopd` and `/usr/local/bin/kbopt` to the compiled-in floor. **Caveat differs per binary:** `kbopd` is the same situation as `kbagents` — `kb-op/kb-dashboard` runs only via `npm run dev` today, no compiled binary exists, so the path is a packaging decision the floor entry anticipates rather than a build that exists. `kbopt` is more real: `kb-tui` already has a working compiled-binary build step (`go build -o kb-tui cmd/main.go`) — only the `/usr/local/bin/kbopt` install path and unit file were missing, not the packaging step itself. [PardhuVarma]
- **`event_type` feature collision in Containment's training data (`scripts/dataset/label.py`)**: First training run scored only 60-73% on `CGROUP`/`SECCOMP` specifically because `credential_access`/`lateral_movement` shared the same engineered `event_type` code and `zone` floor, leaving the trained policy almost nothing to distinguish them by. Gave each of the 6 categories a distinct `event_type` code and retrained; those two levels went to 100% in the retrain, at the cost of `NONE` dropping 100%→82.2% (an accepted trade — over-caution on benign traffic beats a missed threat for a containment system). [Karthik] [PardhuVarma]
- **`.gitignore`'s dataset-raw rule never matched anything**: `kernel-borderlands/scripts/dataset/output/raw` had a stray path prefix relative to where `.gitignore` actually lives, so `scripts/dataset/output/raw/` (real downloaded datasets, real local telemetry samples) was never actually ignored. Fixed to `scripts/dataset/output/raw/`; also added `scripts/zips/` (binary/regenerable training bundles — `kb-aads/marl/checkpoints/*` was added here too but see Changed above, since reversed). [Karthik] [PardhuVarma]
- **Upstream `google-colab-cli` `colab install` crash**: `jupyter_kernel_client` 1.0.0 removed/renamed the `KernelClient` class the CLI's bundled version depends on. Pinned `jupyter_kernel_client==0.15.0` in the tool's isolated `uv` venv to restore the expected API — not a change to this repo's own code, but blocking anyone else using the same `colab` CLI setup for training. [Karthik] [PardhuVarma]
- **Broken relative links to `CPM.md`/`cpm-implementation.md` in `docs/features/hardware-ext/nomad-hardware-appliance.md`**: Four links (§ intro and §14, both former Open Questions) pointed at `[CPM.md](CPM.md)`/`[cpm-implementation.md](cpm-implementation.md)` as if they were siblings in `docs/features/hardware-ext/`, but both files actually live one directory up at `docs/features/`. Fixed all four to `../CPM.md` / `../cpm-implementation.md`. [PardhuVarma]
- **`nomad-hardware-appliance.md` §14 restructured into Resolved / Still Open, three items closed**: Open Question 2 (failure semantics) resolved as a per-deployment policy switch defaulting to fail-closed (fence), mirroring CPM's own "fail closed toward protection" principle; full attacker-cost reasoning moved to `latex/nomad/main.tex` §8. Open Question 3 (relationship to `kb-checker`'s existing quarantine flow) resolved: NOMAD is strictly Stage 5 of 5, below `kb-checker`'s existing software chain, never running in parallel with it — Stage 1 (JIT signature audit/BPF map self-heal) → Stage 2 (`systemctl stop`→`SIGKILL`) → Stage 3 (CPM containment) → Stage 4 (`iptables` drop) → Stage 5 (NOMAD relay cutoff). Open Question 4 (TPM integration point) resolved: two separate TPMs, two separate jobs, neither substitutable for the other — full mechanism detailed in the new `nomad-rootkit-resistance.md` (see Added above) and `latex/nomad/main.tex` §21. A new item 11 was added under Still Open, flagging that the resolution's own PCR-bank selection and TPM2 policy-session wire mechanics aren't yet specced. Original item numbering preserved (not renumbered 1–11 sequentially) to match `nomad-fms.md`'s established convention for this Resolved/Still-Open split. [PardhuVarma]
- **`hardware-ext/README.md` catalog out of date**: `nomad-expense.md` existed in the directory but had no catalog entry at all. `nomad-hardware-appliance.md`'s entry still listed failure semantics as unresolved after the §14 restructure above. Added the missing `nomad-expense.md` entry, added a new entry for `nomad-rootkit-resistance.md`, and updated the `nomad-hardware-appliance.md` entry to reflect the Resolved/Still-Open split. [PardhuVarma]
- **`docs/development/core-control/wire-protocol.md` rewritten in full**: doc carried its own disclaimer that it was stuck at `version = 1`/msg_type 1-2 only, silent on `ProcessExit` (4), `ContainmentCmd` (5), `SensitivePaths` (6), `CPMProtectedExec` (7), and `CWPWorkloads` (8), and was missing the `syscall_entropy_lifetime` field / `start_time_ns` pid-reuse guard the v3 wire bump added. Rewrote against current `kb_bridge.h`/`kb_bridge.c`/`kbd_sensor.c` source: every msg_type now documented with exact packed-struct byte layouts (confirmed `ProcessState`=128B, `ZoneTransition`=40B against the root `CLAUDE.md`'s figures), the msg_type=3 double-booking landmine called out, and CPM/CWP registry-push frame shapes added (both noted as C-side-receiver-only, no Go sender yet). [Claude Code]
- **`kb-control-plane/internal/README.md` still documented a deleted `ssh/` package**: package catalog, package-structure tree, and runtime-flow diagram all described `internal/ssh/` (Wish SSH server + PTY manager) as current, but it was deleted in full on 2026-08-30 when SSH moved to an independent `sshd@kb-operator.service` with `ForceCommand` (see `control-plane-catalog.md` §2.11). Removed the `ssh/` section and tree entry, redrew the runtime-flow diagram to show the external `sshd`/`ForceCommand` path instead. [Claude Code]
- **`docs/project/kb-team.md` and `docs/project/github_discussion_welcome.md` still described `kb-tui` as Go/Bubble Tea/Wish**: both predate the Rust/ratatui rewrite (already reflected correctly in `CHANGELOG.md`'s 2026-07-17 entries and `docs/specifications/README.md`) and the later SSH-to-external-`sshd` migration. Corrected both to Rust/ratatui reached via `sshd@kb-operator.service` + `ForceCommand`, not an in-process Go/Wish server. [Claude Code]
- **`control-plane-catalog.md` claimed `kbctl` was "a README with no source"**: `kb-op/kbctl/` now has a real Go source tree (`main.go`, `cmd_audit.go`, `cmd_policy.go`, `cmd_process.go`, `cmd_ssh.go`, `cmd_stats.go`, `cmd_workload.go`) correctly targeting `kba.sock` — the note was accurate when written but not updated after `kbctl` was actually built. Corrected. [Claude Code]

---

## [8335f23] - 2026-08-02

### Added
- **CPM eBPF-side registration + CWP (Critical Workload Protection) scaffolding (`kb-core`)**: `protected_pids_map`/`protected_exec_paths_map` exec/exit-time registration hooks per `docs/features/CPM.md`, plus a new `protected_workloads_map` (8192 entries, path- or hash-identity tier) implementing `docs/features/CWP.md` §5-6.1 — evaluated strictly after CPM, never merged with it. SHA-256 hashing (`kb_sha256.c`/`.h`) added for hash-based workload identity. `kb-core/tests/test_cwp.py` added; owner-team/justification alerting (§9) explicitly out of scope for this pass. [PardhuVarma]
- **Knowledge graph published (`docs/index.html`, `graphify-out/`)**: A `graphify`-generated knowledge graph of the whole codebase (1,924 nodes, 3,146 edges, 178 communities — vendored `vmlinux.h` BTF excluded so the graph reflects KB's own architecture, not kernel-header noise), linked from the site's new `#graph` section and footer. [PardhuVarma]

---

## [2b35dc9] - 2026-07-30

### Added
- **`kbctl` CLI (`kb-op/kbctl/`)**: New standalone Go CLI (`cmd_audit.go`, `cmd_policy.go`, `cmd_process.go`, `cmd_stats.go`, `cmd_zone.go`) talking to `kb-control-plane` over its gRPC API, plus new `kb.proto` RPCs/messages backing it and `internal/controlplane/grpc.go`/`http.go` handlers to serve them. [Tejaswini4119]
- **Audit chain support (`kb-control-plane/internal/store`, `internal/controlplane`)**: New store-layer methods and gRPC plumbing for querying the audit trail `kbctl audit` reads. [Tejaswini4119]
- **`docs/emergency-backup/` — standalone per-subsystem documentation (`kb-aads.md`, `kb-core.md`, `kb-cp.md`, `kb-op.md`, plus a `README.md` index)**: ~5,300 lines added across several commits, written so each subsystem could be forked and continued independently "in case the team disbands". [PardhuVarma]

---

## [f8096ac] - 2026-07-28

### Added
- **CPM (Containment Policy Manager) authorization gate (`kb-core`)**: `cpm_classify()` in `handle_incoming_containment_cmd()` now rejects containment of PID 1, kernel threads, self-registered sensor components, and a protected-executable registry, per `docs/features/CPM.md`. Classifier lives in userspace C (not in-kernel eBPF) since that's where the actual containment-map write happens — a documented deviation from the spec's literal diagram. Verified live against the real sensor (`kb-core/tests/test_cpm.py`). [PardhuVarma]
- **`kbd.sock`/`kbct.sock` socket split (`kb-core`, `kb-control-plane`)**: Telemetry and containment commands were previously multiplexed on one raw socket connection — a telemetry-volume burst could fill the send buffer, and `write()` returning `EAGAIN` was misread as a dead connection, taking containment delivery down as collateral damage. Split control traffic (containment cmds, sensitive-path/rules pushes) onto a new `kbct.sock`, leaving `kbd.sock` telemetry-only; `kb-control-plane`'s `NewListener()`/`New()` now bind both. Also ignores `SIGPIPE` process-wide. Verified live: a containment command for a protected PID was delivered and rejected correctly while the connection was simultaneously saturated with unrelated telemetry. [PardhuVarma] [Tejaswini4119]
- **`kb-aads` `ControlPlaneClient` streaming/unary mixing guard (`kb-aads/comms/grpc_client.py`)**: Same class of coupling the socket split fixed, at the gRPC-channel layer — a stalled `stream_events`/`stream_alerts` consumer sharing a channel with a latency-sensitive unary call (`submit_decision`, etc.) could delay the unary response. Not live yet (no caller used the streaming methods at the time), fixed pre-emptively. 3 new tests, all passing. [Karthik]

### Fixed
- **Containment-cmd read path reset the wrong bridge fd (`kb-core`)**: The error handler unconditionally reset the global `bridge_fd` instead of whichever fd was actually passed in — found while wiring up the socket split. [PardhuVarma]
- **`test_cpm.py`/`test_restore_ipc.py` silently no-op after the socket split (`kb-core/tests`)**: Both mock-control-plane test drivers bound only `kbd.sock` and sent containment commands there; post-split, `kbd_sensor` no longer reads containment commands from that connection, so these scripts reported success while doing nothing. Updated both to use `kbct.sock`. [PardhuVarma]
- **Stale `kbd.sock`-carries-everything documentation, corrected across the board**: `CPM.md`, `cpm-implementation.md`, `boot_sequence_spec.md`, `wire-protocol.md`, `ipc-v3-wiring.md`, `developer-commands.md`, `worksheet.md`, `specifications/README.md` (which also incorrectly called `kbd.sock` gRPC — `kba.sock` is), `kb-control-plane/README.md`, `cmd/README.md`, and `CLAUDE.md` all still described `kbd.sock` as carrying containment commands or being kb-core's only socket. [PardhuVarma] [Tejaswini4119]
- **`comms/README.md`/`demo/README.md` described ZeroMQ as current**: No `import zmq` exists anywhere in `kb-aads` — ZeroMQ was dropped for Ray before these docs were last touched. New `docs/development/control-aads/kb-events-swarm-ingestion-gap.md` documents, with code citations, that nothing currently consumes `StreamEvents`/`StreamAlerts` at all. Also corrected `aads-intelligence-roadmap.md`'s claim that CPM/CWP is a "scoring engine" (`CPM.md` is explicit it isn't). [Karthik]

---

## [452b64d] - 2026-07-26

### Added
- **Academic/major-project submission materials (`docs/project/major-project-submissions/`)**: PPTX, synopsis PDF, and README/video-link updates for the university major-project review cycle — non-engineering, not detailed individually here. [PardhuVarma]

---

## [8b6a3a2] - 2026-07-24

### Added
- **`kb-aads` actor scaffolding fixed from non-functional to a verified-working skeleton (`kb-aads/agents/`)**: `BaseAgent` was previously decorated `@ray.remote` directly, which throws `ActorClassInheritanceException` the moment any subclass (`HunterAgent`, `PatrollerAgent`, ...) tries to also apply `@ray.remote` — no concrete role actor could actually be constructed. Removed the decorator from `BaseAgent` itself; added `RemoteBaseAgent = ray.remote(BaseAgent)` for roles with no dedicated subclass. Added `AgentState.last_action` and surfaced it via `get_status()`. Added `ExecutorAgent` (`kb-aads/agents/executor.py`) — the first real gateway back to `kb-control-plane`, submitting consensus decisions over `kba.sock` via `submit_decision`. Added `config/agents.yaml` (swarm composition). [Karthik]
- **AADS Intelligence Roadmap (`docs/development/control-aads/aads-intelligence-roadmap.md`)**: Initial roadmap from skeleton agents to trained agents — the per-agent RL/LLM/no-model assignment table, JJE's courthouse-for-sub-agents design, and the phased Phase 0-6 plan this session's Containment training work (see `[Unreleased]` above) partially executes against. [Karthik]
- **Resource management roadmap (`docs/architecture/resource_management_roadmap.md`, `kb-core_system_requirements.md`)**: New architecture docs covering planned resource-management/fleet-deployment direction. [PardhuVarma]
- **Product plan, years 1-2 (`docs/project/kbgoal2yrs.md`)**: Longer-term product roadmap document. [PardhuVarma]

### Fixed
- **Stale cross-references across `docs/`**: Corrected references in `docs/README.md`, `docs/features/README.md`, `docs/reports/README.md`, `docs/specifications/README.md`, `docs/development/core-control/README.md`, and media READMEs after the AADS roadmap and other docs churn left several pointing at moved/renamed files. [Karthik]

---

## [a525f41] - 2026-07-23

### Added
- **`kb-checker` gRPC health-verification channel (`kb-control-plane/internal/checkerclient/`, `proto/checker/`)**: New `checkerclient.go` client plus `checker.proto`/generated Go stubs, letting `kb-control-plane` query `kb-checker`'s health over gRPC — the Go-side half of the `kbd`↔`kb-checker` diagnostic channel referenced in `control-plane-catalog.md`. [Tejaswini4119]
- **Resource management / kb-core system requirements docs (`docs/architecture/`)**: Further build-out of `resource_management_roadmap.md` and `kb-core_system_requirements.md`. [Tejaswini4119] [PardhuVarma]
- **`control-plane-catalog.md` expanded (`docs/development/core-control/`)**: Substantial documentation additions describing the control-plane socket/service catalog — despite several of this date's commit messages referencing "http.go :80xx tcp port" changes, the actual diffs are documentation-only; no `http.go` TCP-port code change landed under these commits. [Tejaswini4119]

---

## [8a3286a] - 2026-07-18

### Added
- **Demo-run telemetry media (`media/demoruns/`)**: READMEs and a tracked demo-run telemetry video, plus general README polish. [PardhuVarma]

---

## [d8c6665] - 2026-07-17

### Added
- **TUI demonstration media (`kb-op/kb-tui/media/`)**: Added `kbtui.gif` and `kbtui.mp4` showing full demonstrations of the new Rust-based `kb-tui` console in action. [PardhuVarma]
- **Verification suite upgrade (`kb-core/tests/test_all_hooks.sh`)**: Refactored the validation script to trigger sequential, multi-stage attack chains (Privilege & Credential, Memory Injection, and C2 Connections) within single processes to test end-to-end `BORDERLANDS` and `COMPROMISED` alert streams. [PardhuVarma]
- **kb-tui rebuilt as a real operator console (`kb-op/kb-tui`)**: Replaced the static ratatui demo screen with a multi-panel console (Processes / Alerts / Agent Activity / Query Console tabs), wired to `kbd`'s `KernelBorderlands` gRPC service over `/run/kb/kba.sock` (tonic + `kb.proto`), with zone-colored process table, filtering, containment action modal, live alert feed, and a typed query-console mini-language. Falls back to a clearly-bannered offline/demo mode with synthetic data if `kbd` is unreachable. [PardhuVarma]
- **Operator-configurable sensitive-path list (`config/policy.yaml`, `kb-control-plane`, `kb-core`)**: Added a `sensitive_paths` key to `policy.yaml`, validated by a new `internal/policy` check (absolute path, fits the 64-byte BPF key, not bare `/`, dedup, capacity-checked against the map's 64-entry limit), pushed to `kbd_sensor` over a new wire message (`KBWireMsgSensitivePaths`, `msg_type=6`, `internal/ipc/sensitive_paths.go`) the moment it connects, and merged into the live `kb_sensitive_paths` BPF map on top of the compiled-in floor. Additive only — the floor can never be narrowed via config. Takes effect on `kbd_sensor` (re)connect, not a live reload. [PardhuVarma]
- **`kb-core/scratchpad/` manual verification scripts**: `run-sensor.sh` (start/restart `kbd_sensor` with logging) and `inspect-bpf-state.sh` (`bpftool`-based dump of live LSM program attachment and `kb_sensitive_paths`/`contained_pids_map` state) — root-requiring, interactive, not part of CI; written specifically because the bugs below were only caught by checking live kernel state, not by build success. [PardhuVarma]
- **AADS Real UDS Verification Guide & Script (`docs/development/control-aads`)**: Added a guide and helper Python script `tests/verify_real_connection.py` to run live connection integration tests between the Python AADS client and the Go control plane. [Karthik]
- **AADS gRPC-over-UDS Client & Ray Actors (`kb-aads`)**: Implemented the Python gRPC client in `comms/grpc_client.py` covering all `kb.proto` methods, created a UDS socket integration test suite with `pytest`, decorated base agents to leverage Ray remote actors, and structured the JJE consensus quorum model. [Karthik]
- **AADS Development Plan (`docs/development/control-aads`)**: Created the comprehensive roadmap and architectural blueprint for migrating python agents to Ray remote actors, implementing JJE consensus quorum, integrating gRPC-over-UDS communications, configuring Ray RLlib multi-agent reinforcement learning, and enabling Ray mTLS cluster encryption. [PardhuVarma]
- **Process Exit Lifecycle (`kb-control-plane`)**: Implemented packet routing and decoding for `MsgTypeProcessExit` (`4`) to immediately flush stale L1 memory cache and L2 SQLite process records upon process termination, preventing PID reuse vulnerabilities. [Tejaswini4119]
- **Process Exit Unit Tests (`kb-control-plane`)**: Added unit and integration tests in `controlplane_test.go` and `wire_test.go` to verify cache eviction and SQL deletion. [Tejaswini4119]
- **SSH Hardening & MCP Specs (`docs`)**: Added Task 4 implementation plan details for SSH Wish hardening and MCP metrics integration. [Tejaswini4119]
- **eBPF Rate Limiting (`kb-core`)**: Implemented hybrid BPF token buckets using Task Local Storage and LRU Hash Maps. 
- **Deep Resource Isolation (`kb-core`)**: Upgraded rate limiting to track limits by `PID + Resource ID` (e.g., Destination IP, Syscall ID) to prevent smoke-grenade sensor evasion.
- **Telemetry Batching (`kb-core`)**: Added `KB_EVT_DROPPED_TELEMETRY` event to accurately aggregate and report dropped payloads to the userspace behavior engine.
- **Rate Limit Isolation Test (`kb-core`)**: Added `tests/isolation_test.py` to test BPF token bucket overload boundaries.
- **Build & Test Scripts (`kb-core`)**: Added helper utilities `build.sh`, `clean.sh`, `test.sh`, and `attach.sh` for simplified operations.
- **Git Authorship Aliases**: Added system-wide bash aliases to `/etc/bash.bashrc` to handle multi-contributor commits cleanly.
- **IPC Restore Test (`kb-core`)**: Added dedicated python integration test script `kb-core/tests/test_restore_ipc.py` to mock control plane containment commands.

### Changed
- **kb-tui declared canonical as Rust/ratatui, not Go (`kb-op/kb-tui`, `kb-op/README.md`, `docs/specifications/operator_interfaces_spec.md`, wiki)**: These four sources disagreed on kb-tui's language/architecture (Go+Bubble Tea+Wish vs. the Rust+ratatui code actually in the repo). Reconciled all of them to Rust/ratatui/tonic, SSH handled entirely by `kbd` (PTY spawn), `kb-tui` talking gRPC over `/run/kb/kba.sock`. [PardhuVarma]
- **Compiled-in LSM sensitive-path floor narrowed (`kb-core/userspace/sensor/kbd_sensor.c`)**: Changed from `{/etc/shadow, /etc/passwd, /etc/sudoers, /root/}` to `{/etc/shadow, /etc/sudoers, /root/.ssh/}`. Dropped `/etc/passwd` — it holds no credential material on a shadow-password system and is opened by nearly every UID-resolving tool (`ls -l`, `id`, `ps`, `sudo`, `ssh`, ...); passive `KB_EV_PASSWD_ACCESS` scoring is unaffected. Narrowed `/root/` to `/root/.ssh/` (the actual credential material, not root's whole home directory). [PardhuVarma]
- **`kb_lsm_file_open` sensitive-path blocking changed from unconditional to containment-gated (`kb-core/ebpf/kbd_sensor.bpf.c`)**: The hard `-EACCES` block now only applies to a process already under operator containment at level ≥2 (Seccomp), matching the containment-level model every other LSM hook in the file already follows (`kb_lsm_bprm_check`, `kb_lsm_socket_connect`, `kb_lsm_socket_bind`, `kb_lsm_file_mprotect`). Passive detection (`kb_handle_openat`'s evidence flags) is unaffected and still fires for every process regardless of containment. **See the Incident section below — this change exists because the previous unconditional version locked `sudo`/PAM out of a live VM.** [PardhuVarma]
- **`MsgTypeContainmentCmd` changed `3` → `5` (`kb-control-plane/internal/ipc/types.go`)**: Now matches the C side's `KB_WIRE_MSG_CONTAINMENT_CMD` (`kb_bridge.h`). See Fixed below — this was a real, previously-shipped bug, not a new choice. [PardhuVarma]
- **`connect_once()` now sets a 2s `SO_RCVTIMEO` (`kb-core/userspace/bridge/kb_bridge.c`)**: The sensor's connect-time blocking reads (rules, sensitive-paths) now fail fast into their documented "use compiled-in defaults" fallback instead of blocking forever. [PardhuVarma]
- **TUI Architecture Updates (`kb-op/kb-tui`)**: Updated `kb-tui` documentation to reflect transition to Ratatui (Rust) and delegation of SSH handling/authentication to the control plane daemon (`kbd`). [PardhuVarma]
- **SSH Hardening Architecture Spec (`docs`)**: Refactored the Task 4 SSH Hardening design spec to move the network-facing SSH server into `kbd` (control plane daemon) and make `kb-tui` a pure subprocess driven over PTY stdin/stdout. [Tejaswini4119]
- **Containment Restore path correctness (`kb-core`)**: Implemented return check for `bpf_map_delete_elem` and `bpf_map_update_elem` in `kbd_sensor.c`, logging deletion/update failures to stderr.
- **Bounded logging outputs (`kb-core`)**: Bounded `cmd->reason` string printing to 64 bytes (`%.64s`) to avoid out-of-bound memory reads when logs print non-null-terminated reason strings.
- **LSM BPF Hook Verifications (`kb-core`)**: Modified LSM socket hook return values (`kb_lsm_socket_connect`, `kb_lsm_socket_bind`, `kb_lsm_file_mprotect`) in `kbd_sensor.bpf.c` to return `-13` (`-EACCES`) instead of `-1` to fix modern kernel verifier rejection.

### Fixed
- **`kbd_sensor` hung forever at startup whenever `kbd` was reachable (`kb-core/userspace/sensor/kbd_sensor.c`)**: `read_rules_from_bridge()` did a blocking `read()` with no timeout, waiting for a rules frame that `kb-control-plane`'s production code (`SendRulesPayload`) never actually sends (it's only invoked from a test). The sensor would never reach eBPF loading. Fixed via the `SO_RCVTIMEO` change above. [PardhuVarma]
- **The `sensitive_paths` wire frame was silently discarded before the new reader ever saw it (`kb-core/userspace/sensor/kbd_sensor.c`)**: `read_rules_from_bridge()` blindly consumes whatever frame arrives next on the wire, regardless of its real `msg_type` — and since `kbd` sends the sensitive-paths push (the only frame actually sent at connect time) before the sensor gets around to reading for it, the rules reader ate it, saw a mismatched `msg_type`, and discarded it, leaving nothing for `read_sensitive_paths_from_bridge()` to read later. Fixed with a stash-and-reuse mechanism: `read_rules_from_bridge()` now stashes a non-rules frame it doesn't recognize instead of freeing it, and `read_sensitive_paths_from_bridge()` checks that stash first before attempting a fresh read. [PardhuVarma]
- **The LSM sensitive-path block had never actually been enforcing, despite being documented as "loaded & active" (`kb-core/ebpf/kbd_sensor.bpf.c`)**: `bpf_d_path()` writes a NUL-terminated path into `path_buf` but doesn't guarantee the buffer's tail past the terminator is zeroed, while `kb_sensitive_paths`' `bpf_map_lookup_elem` compares the full fixed-size 64-byte key, not just the string. Leftover stack bytes past the terminator meant even an exact, correctly-registered entry (including the compiled-in floor) silently never matched. Confirmed via `bpf_trace_printk`: the path printed correctly, but the direct map lookup still reported `found=0`. Fixed in `kb_lsm_file_open` by explicitly zero-filling `path_buf[len..63]` after `bpf_d_path()` returns. This means this specific block genuinely never enforced anything until this fix — not a regression, a pre-existing latent bug. [PardhuVarma]
- **`SetContainment` (used by `kb-tui`/`kbctl`) had never actually applied containment on the sensor side (`kb-control-plane/internal/ipc/types.go`)**: `MsgTypeContainmentCmd` was `3` in Go but the C side's `handle_incoming_containment_cmd()` checks for `KB_WIRE_MSG_CONTAINMENT_CMD` (`5`) — every containment command was silently dropped by the sensor. `kbd` logged a successful `SET_CONTAINMENT_*` audit entry regardless (the sensor never NACKs), masking the failure completely — `contained_pids_map` stayed empty no matter how many containment calls were made. Fixed by changing Go's constant to `5`. Confirmed via a live test: a test PID put into Seccomp containment via `kb-tui` had its sensitive-path access correctly blocked afterward, while an uncontained process reading the same file continued to succeed. [PardhuVarma]

### Removed
- **Blanket, unconditional `/etc/passwd` LSM file-block (`kb-core`)**: removed from the compiled-in floor entirely (see Changed above) — passive `KB_EV_PASSWD_ACCESS` scoring detection is kept.
- **Kafka Removal in AADS Subsystem (kb-aads/)**: removed kafka legacy code to implement low latency native uds communications fr inter-agent, and ray clusters.

---

## Incident: `sudo`/PAM lockout during live LSM-block verification (2026-07-17)

**What happened**: While verifying the zero-padding fix (see Fixed above), the LSM sensitive-path block fired correctly for the first time in this repo's history — and immediately blocked reads of `/etc/sudoers` and `/etc/shadow` system-wide, because the block was unconditional (applied to every process, not just contained ones) at the time this was tested. This broke `sudo` itself (`sudo: unable to open /etc/sudoers: Permission denied`) and PAM password authentication generally (`su`, `pkexec`, `login` — anything reading `/etc/shadow`) on the live test VM.

**Why recovery was hard**: Every standard privilege-escalation path needs one of the two now-blocked files: `sudo` reads `/etc/sudoers` on every invocation; PAM's `pam_unix` module reads `/etc/shadow` to verify a password, so even `pkexec` (a different authorization mechanism) failed at the PAM step. With no already-authenticated root shell or key-based root SSH login available, the only recovery path left was a full VM restart from outside the terminal (hypervisor/cloud console) — a reboot clears all BPF programs, including the stuck LSM hook, since they don't persist across boots.

**Root cause**: `kb_lsm_file_open`'s sensitive-path check was unconditional — it applied to every process on the system, not scoped to processes an operator had actually placed into containment. This was inconsistent with the containment-level model the code's own comments already documented (`kb-core/ebpf/kbd_sensor.bpf.c`'s "Containment level semantics" block says sensitive-path blocking belongs to level-2 containment) and inconsistent with every other LSM hook in the same file, all of which are already containment-gated.

**Resolution**: Made `kb_lsm_file_open` containment-gated, matching the rest of the file (see Changed above). Verified post-fix: `sudo whoami` succeeds cleanly with `kbd_sensor` actively running and enforcing; a deliberately-contained test process has its sensitive-path access blocked as intended, while every other process is unaffected.

**Takeaway for future work on this hook**: any change to `kb-core`'s LSM blocking logic should be tested by containing a *disposable* test process first, never verified by directly triggering system-wide "does the floor list block this path" checks against real `/etc/sudoers`/`/etc/shadow` on a machine you still need `sudo` on.

---

## Major Subsystem Milestones (Chronological History)

### July 2026

*   **2026-07-17** - `[d8c6665]` Add `kbtui.gif` and `kbtui.mp4` demonstrations of the new Rust-based TUI console (*PardhuVarma*)
*   **2026-07-17** - `[59b91a7]` Upgrade `test_all_hooks.sh` with realistic single-process sequential attack chains (Chain A, B, and C) to trigger SUSPICIOUS, BORDERLANDS, and COMPROMISED transitions for live alert stream verification (*Claude Code*)
*   **2026-07-17** - `[a387415]` `kb-tui` rebuilt as a real multi-panel ratatui/tonic operator console; `sensitive_paths` made operator-configurable end-to-end (`policy.yaml` → `kbd` → wire → sensor BPF map); found and fixed four live pre-existing bugs (connect-time hang, sensitive-paths frame collision, LSM zero-padding match failure, containment `msg_type` mismatch that meant `SetContainment` never actually worked); made LSM sensitive-path blocking containment-gated instead of blanket after a live `sudo`/PAM lockout incident during verification, requiring a VM reboot to recover (*Claude Code*)
*   **2026-07-16** - `[f4de926]` AADS gRPC-over-UDS client, UDS test suite, Ray remote actor base agents, JJE consensus, real connection verification script, and development plan (*Karthik*)
*   **2026-07-15** - `[7728598]` process exit lifecycle implementation, cache flushes, unit tests, and refactored SSH daemon-side architecture spec (*Tejaswini*); updated TUI README for Ratatui and kbd SSH delegation (*PardhuVarma*)
*   **2026-07-14** - `[faa87cb]` eBPF token bucket rate limiting, telemetry batching, and deep resource isolation (*PardhuVarma*)
*   **2026-07-14** - `[52b6a7a]` gap work implementation, LSM hook return corrections & IPC restore tests (*PardhuVarma*)
*   **2026-07-14** - `[3e3f790]` readme updates (*Rupa Karedla*)
*   **2026-07-13** - `[5d2a5a4]` git authorship documentation updates (*PardhuVarma*)
*   **2026-07-13** - `[55755dd]` Controlplane updates (*Tejaswini*)
*   **2026-07-13** - `[895a817]` CPM, CWP & Gap Fixation Updates (*PardhuVarma*)
*   **2026-07-11** - `[0379903]` `kb-control-plane`: wire IPC listener, enforcer construction, Contain() call sites (*Tejaswini*)
*   **2026-07-11** - `[72d89e7]` `kb-core`: containment restore and LSM enforcement coverage (*PardhuVarma*)
*   **2026-07-11** - `[77d1101]` `kb-core` sensor: autoload for TLS/SSL uprobes to resolve attachment failures (*PardhuVarma*)
*   **2026-07-11** - `[c4d5f04]` `kb-control-plane`: refined isProcessRunning argument matching to prevent false positives (*Tejaswini*)
*   **2026-07-11** - `[adfcc29]` `kb-control-plane`: normalized raw scoring values for HTTP/SSE feeds (*Tejaswini*)
*   **2026-07-11** - `[e87a070]` `kb-dashboard`: dynamic health indicators and performance metrics from daemon (*Rupa Karedla*)
*   **2026-07-11** - `[8538cbd]` `kb-dashboard`: Redesigned professional SOC console with sidebar, health panel, alert feed, and audit terminal (*Rupa Karedla*)
*   **2026-07-11** - `[8cc9ab7]` `kb-dashboard`: Initialized Vite-React-TypeScript dashboard with premium security telemetry visualization (*Rupa Karedla*)
*   **2026-07-09** - `[0852575]` `kb-checker`: removed Apache Kafka references in favor of ZeroMQ and Ray IPC (*Karthik*)
*   **2026-07-09** - `[090d733]` `kb-checker`: implement hard fallback containment locks in report recovery (*PardhuVarma*)
*   **2026-07-09** - `[49a2f51]` `kb-checker`: integrate systemd service controls in recovery (*PardhuVarma*)
*   **2026-07-09** - `[fadcc16]` `docs`: added Secure Boot-up Tampering Containment and Workload Gating Section (*PardhuVarma*)
*   **2026-07-08** - `[62ce504]` `kb-checker`: single-instance PID locking and signal cleanup (*PardhuVarma*)
*   **2026-07-08** - `[c399f12]` `kb-checker`: eBPF hook performance latency monitoring (*PardhuVarma*)
*   **2026-07-08** - `[898dbe9]` `kb-checker`: self-healing BPF map state integrity audits (*PardhuVarma*)
*   **2026-07-08** - `[cb022b8]` `kb-checker`: dynamic SHA-256 eBPF bytecode instructions hashing (*PardhuVarma*)
*   **2026-07-08** - `[ed27291]` `kb-checker`: modularized safety checker and status gRPC server over Unix Domain Sockets (*PardhuVarma*)
*   **2026-07-08** - `[72a79b8]` `kb-checker`: Rust safety daemon validation loops and gRPC/REST clients (*PardhuVarma*)
*   **2026-07-08** - `[a85f675]` `kb-core`: eBPF tasks for containment feedback loops and exit events (*PardhuVarma*)
*   **2026-07-07** - `[8c85a72]` `kb-op`: Integrated `kb-mcp` subsystem tool suite for operators (*PardhuVarma*)
*   **2026-07-07** - `[16395be]` `docs`: eBPF event rate limiting design specifications (*PardhuVarma*)
*   **2026-07-06** - `[02f0e3d]` `kb-core` & `kb-control-plane`: Integrated BPF LSM, behavior state machine, TLS uprobes, and fixed L2 DB race (*PardhuVarma & Tejaswini*)
*   **2026-07-04** - `[41df005]` `kb-control-plane`: SQLite3 database checks, core socket fallbacks, and major Go wiring updates (*Tejaswini & PardhuVarma*)
*   **2026-07-03** - `[0a691f9]` `kb-core`: eBPF scoring engine (`kb_scoring.c/.h`) and Unix socket streaming (`kb_bridge.c/.h`) (*PardhuVarma*)
*   **2026-07-01** - `[c11ec39]` `docs`: Rupa On-boarding and team info update (*Rupa Karedla & PardhuVarma*)
*   **2026-07-01** - `[0d539b9]` Userspace reorganization and build improvements (*PardhuVarma*)

### June 2026

*   **2026-06-26** - `kb-control-plane`: Protofile definition and daemon initialization (*Tejaswini*)
*   **2026-06-26** - `kb-checker`: Kafka topics and communication setup (*Karthik*)
*   **2026-06-26** - `kb-core`: Hook-2 syscall tracking (*PardhuVarma*)
*   **2026-06-26** - Karthik onboarding to the kernel-borderlands team (*Karthik & PardhuVarma*)
*   **2026-06-25** - `kb-core`: CO-RE & Cross Kernel Portability specs (*PardhuVarma*)
*   **2026-06-25** - `kb-aads`: Swarm Initial Setup (*Karthik*)
*   **2026-06-25** - `kb-core`: Initial eBPF tracepoints program (*PardhuVarma*)
*   **2026-06-25** - Tejaswini onboarding to the kernel-borderlands team (*Tejaswini & PardhuVarma*)
*   **2026-06-19** - Documentation for hook points and monitoring strategies (*PardhuVarma*)
*   **2026-06-11** - AADS Swarm technical requirements (*PardhuVarma*)

### May 2026

*   **2026-05-02** - Initial project repository setup, initial README, and project description (*PardhuVarma*)

---

## Contributor Breakdown

### Pardhu Varma (`PardhuVarma Konduru`)
- Lead developer of eBPF instrumentation (`kb-core`), including LSM hooks, tracing, and dynamic skeletons.
- Architected the Rust safety watchdog (`kb-checker`).
- Set up project specs, boot sequences, and collaborative roadmaps.

### Rupa Karedla (`Rupakaredla`)
- Architected and built the React-TypeScript SOC Dashboard (`kb-dashboard`).
- Integrated HTTP telemetry and SSE feeds.
- Contributed to userspace documentation.

### Tejaswini (`Tejaswini4119`)
- Built the Go daemon control plane (`kb-control-plane`) and CGO bindings.
- Configured SQLite3 local storage and gRPC interfaces.
- Implemented event normalizations.

### Karthik (`Karthik21002`)
- Researched agent defense swarm (`kb-aads`).
- Configured ZeroMQ, Kafka topics, and Ray IPC.
